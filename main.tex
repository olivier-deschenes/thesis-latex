\documentclass[12pt]{article}

% Core packages
\usepackage{amsmath,amssymb}
\usepackage{tikz-cd}
\usepackage{multicol}
\usepackage{etoolbox}
\usepackage{fancyhdr}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage[labelfont=bf]{caption}
\usepackage{amsmath}
\usepackage{optidef}
\usepackage{tikz}
\usepackage{geometry}
\usepackage{acro}
\usepackage[hidelinks]{hyperref}
\usepackage{cleveref}
\usepackage{siunitx}
\usepackage{xurl}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{csquotes}
\usepackage{makecell}
% biblio

% --- Bibliography Setup ---
\usepackage[
  backend=biber,
  style=authoryear-comp,
  maxcitenames=2,
  dashed=false
]{biblatex}
\DeclareFieldFormat{titlecase}{#1}
\addbibresource{references.bib}
\pagestyle{fancy}
\graphicspath{{images/}}
\preto{\section}{\clearpage}
\setlength{\headheight}{15pt}
\renewcommand{\sectionmark}[1]{\markboth{#1}{}}


% acro

\DeclareAcronym{osm}{
  short = OSM,
  long  = OpenStreetMap
}

\DeclareAcronym{cagr}{
  short = CAGR,
  long  = Compound annual growth rate
}

\DeclareAcronym{vrp}{
  short = VRP,
  long  = Vehicle Routing Problem,
}

\DeclareAcronym{scvrp}{
  short = SCVRP,
  long  = Symmetric Capacitated Vehicle Routing Problem,
}

\DeclareAcronym{psclp}{
  short = PSCLP,
  long  = Partial Set Covering Location Problem,
}

\DeclareAcronym{kpi}{
  short = KPI,
  long  = Key Performance Indicator,
}

\sisetup{range-units = single}

% Header styles
\fancyhf{}

% Header setup
\lhead{\nouppercase{\leftmark}}
\rhead{\thepage}

% Paragraphs
\setlength{\parindent}{0pt}
\setlength{\parskip}{1\baselineskip}

\title{Ports, Boats and Packages \\ \large A Two-Stage Optimisation of Urban Waterborne Logistics in Bergen}
\author{Olivier Deschênes}

\date{\today}

\begin{document}

\newgeometry{
  top=2.5cm,
  bottom=2.5cm,
  left=2.5cm,
  right=2.5cm
}

\begin{titlepage}
  \makebox[\textwidth]{%
    \begin{minipage}[t]{0.45\textwidth}
      \vspace{0pt}
      \includegraphics[width=1.5cm]{logos/nhh.png}%
      \quad
      \includegraphics[height=1.5cm]{logos/hec.png}%
    \end{minipage}%
    \hfill
    \begin{minipage}[t]{0.45\textwidth}
      \vspace{0pt}
      \raggedleft
      {\small Norwegian School of Economics\par}
      {\small HEC Montréal\par}
      {\small Bergen, Spring 2026}
    \end{minipage}%
  }

  \centering
  \vspace*{\fill}
  {\textbf{\LARGE Ports, Routes and Boats}\par\vspace{-0.5\baselineskip}}
  {\large{\textit{\large A Three-Stage Optimisation of Urban Waterborne Parcel Logistics in Bergen}}\par}
  \vspace*{\fill}
  {\large{\textbf{Olivier Deschênes}}\par\vspace{-0.5\baselineskip}}
  {\bfseries
    {\normalsize Supervisors: Jean-François Cordeau (HEC Montréal)\par\vspace{-1\baselineskip}}
    {\normalsize Julio C. Góez (NHH)\par\vspace{-1\baselineskip}}
    {\normalsize Stein W. Wallace (NHH)\par\vspace{-1\baselineskip}}
  }
  \vspace*{\fill}
  {\normalsize Master thesis, Economics and Business Administration\par\vspace{-1\baselineskip}}
  {\normalsize Major: Energy, Natural Resources and the Environment\par}
  \vspace*{\fill}
  {\normalsize NORWEGIAN SCHOOL OF ECONOMICS\par\vspace{-1\baselineskip}}
  {\normalsize \&\par\vspace{-1\baselineskip}}
  {\normalsize HEC MONTRÉAL\par}
  \vspace*{\fill}
  \begin{minipage}{0.9\textwidth}
    {\small This thesis was written as a part of the Master of Science in Economics and Business Administration at NHH. Please note that neither the institution nor the examiners are responsible -- through the approval of this thesis -- for the theories and methods used, or results and conclusions drawn in this work.}
  \end{minipage}
\end{titlepage}

\onehalfspacing

\section{Acknowledgements}

\section{Licenses and Attributions}

Unless otherwise stated, all maps in this thesis were produced in QGIS using CARTO basemap tiles and OpenStreetMap map data. The required map attribution is: \textcopyright{} CARTO, \textcopyright{} OpenMapTiles and \textcopyright{} OpenStreetMap contributors.

\section{Abstract}

\clearpage
\tableofcontents
\clearpage

\section{Introduction}
\label{sec:introduction}

Global parcel volumes have increased substantially over the last decade, largely driven by e-commerce, and several industry forecasts expect continued growth toward 2030 \parencite{pitney_bowes_index_2023,wef2020}. At the same time, many urban centres are becoming more difficult environments for conventional truck-based delivery \parencite{lastmileimprove,su14084701}. Cities increasingly regulate freight access through measures such as low-emission zones, delivery time windows, and pedestrian areas, while dense or historic street networks can also limit the suitability of large vehicles \parencite{su151612266,MILENKOVIC2024101157,sustainableverlinde}. The last mile is therefore becoming both more constrained and more expensive at the very moment its volume reaches historic highs.

If trucks have less and less room to operate and the parcels still need to arrive, the question becomes what alternative can carry them. One common response is to switch to smaller vehicles such as electric vans and cargo bikes, which work well in dense urban cores where deliveries are clustered, but scale poorly to cities that are larger, hillier, or more spread out \parencite{NARAYANAN2022278}. Another answer predates these vehicles entirely. Cities have moved goods on water for most of their existence, and several are now reviving the practice \parencite{dhlboats,fludis}.

Bergen, with a network of fjords and inlets that reaches into nearly every populated district, has potentially the geography to be next.

\subsection{Problem description}
\label{subsec:problem-description}

\subsection{Research question}
\label{subsec:research-question}

\subsection{Scope}
\label{subsec:scope}

\subsection{Thesis structure}
\label{subsec:thesis-structure}

\section{Background}
\label{sec:background}

\subsection{Urban Parcel Delivery and Last-Mile Logistics}
\label{ssec:urban-parcel-delivery}

With an expected \num{225} billion global parcels in 2028, up from \num{161} billion in 2022 \parencite{pitney_bowes_index_2023}, delivery companies will have to adapt fast to a world that keeps ordering more and more parcels every year. Over long distances, more container ships can still be used, more trains can still be used on land, and more trucks can still bring parcels to the distribution centres. But when it comes to city centres, at some point there is simply no space left for trucks to drive in. The supply chain is built to scale in response to demand that keeps rising, but the bottleneck is simple. As the chain does its job, the vehicles used to transport the goods to the next link get smaller and smaller.

At the end of the chain, on what is called `Last-mile`, the jobs is now on medium to small trucs to do the job. Its their jobs to navigate in small cities environments, that were simetimes, mostly in Europe, not built for veihcule, therefore having narrow roads with sharp corners. This factors, combime with the repartition of the demand accross the cities, forces the trucks to be smaller, but to still have to do many deliveries to reach all the customers, forcing the needs of more trucks.

At the end of the chain, on what is called the \textit{last mile}, the job falls on medium to small trucks. The last mile is widely recognised as the most expensive part of the supply chain, often accounting for more than half of the total delivery cost \parencite{savelsbergh2016}. It is also the most operationally complex. Routes have many short stops, vehicles are rarely full, and time windows are tight. The trucks also have to navigate dense city environments that, especially in Europe, were never designed for vehicles, with narrow roads and sharp corners. Combined with demand spread across the whole city, this forces the trucks to be smaller, but they still need to make many deliveries to reach all the customers, which in turn requires more trucks.

In the meantime, cities that are often already experiencing higher than ever congestion from private car use are now also facing more and more delivery trucks on their roads. Delivery vehicles contribute disproportionately to urban congestion relative to their share of total traffic. They also cause the same environmental and material impacts as passenger cars, often more. They are harder to electrify than cars, so they continue to generate high levels of CO2 and noise pollution while operating close to where people live. Even when they are electric, trucks are much heavier than passenger cars, which means more wear and tear on the roads and higher levels of dust pollution. All of these costs are external to the carrier. The carrier pays for fuel and labour. The city and its inhabitants pay for the rest.

Forced to act on it, some cities and governments are creating more and more projects and regulations to help combat this. The research field that groups these efforts together is known as \textit{city logistics} \parencite{savelsbergh2016}. The goal is simple. Replace one long polluting last leg of delivery with a shorter, cleaner and often shared one. They can do this in many ways. One option is to set up micro consolidation centres at the edge of dense areas, from which cargo bikes or other small electric vehicles can handle the last segment. If this is not feasible, truck traffic can be moved to off-hour times during the day, in order to spread it out. Another option is to restrict high-emission vehicles from entering certain parts of the city, often the dense and walkable city centre. Finally, a popular solution that can work for all parties is to consolidate household deliveries into small and shared pickup points across the city. This makes shipping companies more efficient since more demand is assigned to a single delivery spot, and it allows delivery trucks to serve only a few concentrated locations, which are often places of commerce. In Norway, both PostNord and Posten have already rolled out automated parcel lockers along these lines, and they are now used by a large share of the population \parencite{toiparcellockers, posten_hours, portnord_hours}.

Many of these solutions involve the participation of both shipping companies and the population. The population plays a crucial role, because it often has to cover a portion of the delivery route itself. Even if a customer orders a package and would prefer to have it delivered to their address, the package may instead need to be picked up somewhere else, close to its original delivery point. In a perfect world, the customer would be able to make this pickup without causing any extra pollution. For this to work, the pickup points need to be reasonably close to where everyone lives. As more cities face the same challenge, a planning concept known as the \textit{15-minute city} has emerged \parencite{moreno2021}. The idea is simple. All essential services should be reachable within a \qty{15}{\minute} walk for every resident. This thesis adopts the same \qty{15}{\minute} threshold as the base walking time for its port selection model.

\subsection{Bergen as a Constrained Logistics Environment}
\label{ssec:bergen}

This thesis will be based on the city of Bergen in Norway. As it sits on Norway's west coats, the city is directly inbtween the North Sea and the mountains. As it one of many reaosns why tourists comes to visit the city, its seven iconic montains, combine with its many fjords, spread the city's population around them. The population thefore mostly leaves close to the coast, and farder in land rapidly becomes hight montrains valley. The city is also splitted into eight districts (\textit{bydeler}).

This thesis is based on the city of Bergen, located on the west coast of Norway. The city sits directly between the North Sea and the mountains that frame it. Bergen is famously known as the city of seven mountains, and these iconic peaks, combined with the fjords that cut deep into the urban area, leave only narrow strips of land where the population can live. People therefore live mostly close to the coast, while the land rises quickly into high mountain valleys further inland. The city is split into eight districts (\textit{bydeler}): Arna, Bergenhus, Fana, Fyllingsdalen, Laksevåg, Ytrebygda, Årstad and Åsane.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.5\textwidth]{maps/districts.pdf}
  \caption{Total population of Bergen in 2020 aggregated by district.}
  \label{fig:district_population}
\end{figure}

These districts can be seen geographically in Figure~\ref{fig:district_population}, and the full population breakdown by district is shown in the \nameref{sec:appendix} in Figure~\ref{anx:population-distribution}. They can be grouped into two sets. The first group, made up of Bergenhus, Årstad, Fyllingsdalen, Laksevåg and Ytrebygda, sits in the central and coastal part of the city, around the main fjords, and accounts for about \qty{65}{\percent} of Bergen's total population. The second group, Åsane, Arna and Fana, lies on the outer edges of the municipality. Because this second group has a much larger area with lower population density and mostly mountainous terrain, its road network is also less dense than in the first group.

Bergen also has significant elevation changes across its urban area, again caused by the surrounding mountains that force the population into limited coastal area. Because flat coastal land runs out quickly, a share the population lives at higher elevations on the slopes leading up from the shore. This makes walking distance in Bergen harder to evaluate, as slope strongly affects walking speed. According to Tobler's Hiking Function \parencite{walkingstats}, a walk that takes 15 minutes on flat ground stretches to 21 minutes at a \qty{10}{\percent} incline. Walking is fastest at a \qty{5}{\percent} decline, where the same walk takes only 12.5 minutes, but steeper declines of \qty{10}{\percent} bring the pace back down to 15 minutes. Standard pedestrian network analysis tends to ignore elevation \parencite{noslope}, even though it is known to have a real effect on walking time in hilly cities like Bergen \parencite{slopegood}. Since the goal of this thesis is to explore the feasibility of a waterway shipping system rather than to plan a real operational schedule, slope is ignored in the walking time calculations and identified as a direction for future work.

Moreover, Bergen's weather is known for being one of the rainiest city in Europe. A lot of rain will cause the city to have wet pavements and infrastructures that could make it less favoralbe for long walking distance. However, as for the splop. this limitations will be ignored in this work, and the hypothesis that the population living there is accomodated to the weather. Nonetheless, in a real world scenario, it would be interesting to see the impact of weather in the walking compared to driving to this port locations.

The same geography that constrains road delivery offers a natural alternative. Because the population lives mostly along the coast, residents are already close to potential waterways. Roads, on the other hand, have to fight the geography. They either go around mountains and fjords, cross them with bridges, or cut through them with tunnels. Every workaround adds distance and creates a chokepoint. For boats, those same fjords are direct, open corridors between districts. What slows down road delivery is exactly what makes water delivery efficient.

Bergen also has the existing infrastructure to support such a system. As the second largest city in Norway, it hosts both the second largest port \parencite{bergenport} and the second largest cargo rail station \parencite{bergenrail} in the country, both located in the central area of the city. In practice, this means that the heavy logistics needed to feed a waterborne parcel system is already in place, and would not need to be built from scratch.

\subsection{Waterborne Transport as an Urban Freight Alternative}
\label{ssec:waterborne-urban-freight}

While the trucking industry today dominates urban freight to the point where it can seem impossible to imagine cities working without it, large urban centres had to organise their cargo flows for centuries before trucks existed. Amsterdam, Paris and London all relied on their canal networks to move goods into and out of the city until well into the 20th century, while Venice never moved away from water at all. The expansion of the road network during the 20th century changed this, as trucks offered a flexibility and reach that canal and river freight could not match.

In recent years, several companies have started to revive this more traditional way of handling cargo. DHL is running waterway delivery trials in cities like Venice, Lagos and Berlin \parencite{dhlboats}, while Fludis is operating on the Seine in Paris \parencite{fludis}, both with the goal of reducing the number of trucks in city centres and the pollution that comes with them. The Paris case illustrates the approach well. The city is dense and difficult for trucks to navigate, which gave Fludis the opportunity to use the Seine as a backbone for its operations. The idea is simple. Parcels are first loaded onto small electric delivery bikes, the bikes are then loaded into a barge-like boat, and the boat carries them upriver to a drop-off point. From there, the bikes handle the last-mile delivery and then return to the boat for the trip back to the main warehouse. This setup removes the short-range constraint of the bikes while keeping trucks out of the city itself.

Bergen has the right combination to do the same, and in some ways an even stronger one. Where Paris works with a single river, Bergen has a network of fjords and inlets that already touches almost every populated district. Using these waterways to reach the population by boat would free the streets of the polluting trucks that currently handle this work. A boat network would not be subject to peak-hour traffic, to the road bottlenecks created by Bergen's terrain, or to the geographic constraints that make some parts of the city hard to reach by road in the first place. On top of this, the fjords cut deep into the city in several places, which allows boats to reach areas that sit well inland from the open coast.

\subsection{Theory}
\label{subsec:literature-review}

\subsection{Facility Location Problems}
\label{ssec:flp}

Facility location problems (FLPs) are a family of optimisation models that decide where to open facilities so that a set of demand points is served in the best possible way. The classical setup takes a set of candidate locations, a set of demand points, and a distance or cost for every demand-facility pair, and then asks which subset of locations should be opened. What counts as ``best'' depends on the objective, which usually balances the cost of opening facilities against the cost of serving demand from them.

The literature has converged on a small number of classical formulations. The \(p\)-median problem, introduced by \cite{hakimi1964optimum}, opens exactly \(p\) facilities and minimises the total weighted distance between demand points and their nearest facility. The \(p\)-center problem, also from \cite{hakimi1965optimum}, has the same structure but minimises the maximum distance instead of the total, which makes it useful when worst-case service matters. The set covering location problem (SCLP) of \cite{toregas1971location} takes a different angle. It fixes a coverage distance, considers a demand point covered if a facility is open within that distance, and minimises the number of open facilities under the constraint that every demand point is covered. The maximum covering location problem (MCLP) of \cite{church1974maximal} relaxes the requirement to cover everyone, fixes the number of facilities at \(p\), and maximises the demand covered within the distance threshold.

The partial set covering location problem (PSCLP) sits between the SCLP and the MCLP. Like the SCLP it minimises the number of open facilities and uses a coverage distance to define what counts as served. Like the MCLP it accepts that not every demand point will be covered. The constraint is instead that a target share, or absolute amount, of demand must be covered \parencite{psclp}. The PSCLP is therefore well suited to settings where full coverage is either too expensive or simply not realistic, and where the planner can specify a service level rather than a strict requirement.

Covering models are common in last-mile logistics, where deciding where to put pickup points, parcel lockers, or micro-depots is a central part of network design. \cite{schwerdfeger2020optimizing} use covering ideas to place mobile parcel lockers in urban areas, while \cite{deutsch2018parcel} study the location of pickup points under capacity constraints. The trade-off between the number of facilities and the share of customers within a comfortable walking distance is a recurring theme, which makes the partial covering family a natural fit \parencite{boyer2009last}.

\subsection{Vehicle Routing Problem}
\label{ssec:vehicle-routing-urban-freight}

The vehicle routing problem (VRP) was first formulated by \cite{dantzig1959truck} as a generalisation of the travelling salesman problem to the case where a fleet of vehicles, instead of a single one, must serve a set of customers from a central depot. The base version asks how to route the vehicles so that every customer is visited exactly once, every vehicle starts and ends at the depot, and the total travel cost is minimised. Since then the VRP has grown into one of the most studied problems in combinatorial optimisation, with a large literature on both modelling and solution techniques \parencite{VIDAL2020401}.

Many variants of the VRP have appeared over the years, each one adding the features that a specific application requires. The capacitated VRP (CVRP) gives each vehicle a maximum load and forces the total demand on a route to stay below that limit. The VRP with time windows (VRPTW) requires every customer to be visited within a given interval. The multi-depot VRP allows several depots, the pickup and delivery VRP combines both directions of flow, and the heterogeneous fleet VRP works with vehicles of different types and capacities. A second distinction that matters for this thesis is between symmetric and asymmetric travel cost matrices. The asymmetric case appears mostly in road networks with one-way streets, while in maritime and waterway settings the travel time between two points is usually the same in both directions, which makes the symmetric version the standard choice \parencite{BRAEKERS2016300}.

Vehicle routing has been applied widely to urban freight, where the volume of last-mile deliveries has grown together with e-commerce and has put pressure on city traffic, emissions, and noise levels. \cite{cattaruzza2017vehicle} review the VRP variants that come up most often in city logistics and stress the role of capacity, time windows, and limited delivery zones in shaping the models. \cite{crainic2009models} discuss how urban consolidation centres and dedicated fleets can be combined with routing models to reduce truck traffic in city centres. The use of alternative transport modes such as electric vans, cargo bikes, and inland waterway boats is part of the same line of research, since each mode brings its own constraints to the routing model \parencite{savelsbergh2016city}.

Solving the VRP exactly is hard. The problem is NP-hard, and exact methods such as branch-and-cut or branch-and-price are limited to instances with at most a few hundred customers \parencite{BRAEKERS2016300}. In practice, most realistic instances are solved with heuristics and metaheuristics. Classical construction heuristics include the savings algorithm of \cite{clarke1964scheduling} and various insertion methods. Metaheuristics such as tabu search, large neighbourhood search, and hybrid genetic algorithms produce high quality solutions in reasonable time and are at the core of most modern VRP solvers \parencite{vidal2013heuristics}. Open-source tools such as Google OR-Tools and VROOM make these techniques accessible without having to implement them from scratch.

\subsection{Vehicle Scheduling Problem}
\label{ssec:demand-uncertainty-scenarios}

The vehicle scheduling problem (VSP) takes a set of trips with fixed start and end times and asks how to assign them to vehicles so that every trip is covered and the total cost is minimised \parencite{freling2001models}. Each trip is described by a duration, a starting point, and an ending point, and a vehicle can do a sequence of trips only if it has enough time to move between the end of one trip and the start of the next. Costs typically combine a fixed cost for using a vehicle with a variable cost related to driving and idle time. The basic version is the single-depot VSP, which can be solved in polynomial time, while the multi-depot version is NP-hard \parencite{bunte2009overview}.

The VSP usually appears after the routing decisions in transportation planning. In the standard planning hierarchy used in public transport and urban freight, decisions move from strategic choices about the network, to tactical choices about which routes to operate and how often, and finally to operational choices about which vehicle does which trip and when \parencite{ibarra2015planning}. Vehicle scheduling sits at the boundary between tactical and operational planning. Once the set of trips to run is known, the VSP turns those trips into a usable schedule by matching them with the available fleet.

When the trips are fixed loops of similar structure and the only question is how many loops each vehicle can perform within a working day, the VSP reduces to a problem that is close to bin packing. Each vehicle is a bin with a capacity equal to the available working time, and each loop is an item with a size equal to its duration. The goal is to pack the loops into as few bins as possible, or to fit a target set of loops into a fixed number of bins. Classical bin packing and its many variants have been studied for decades and remain an active research area, for both exact methods and heuristics \parencite{delorme2016bin, munien2021metaheuristic}.

\section{Data}
\label{sec:data}

The data used in this thesis comes from three independent sources, each addressing a distinct aspect of the problem: the spatial distribution of Bergen's population, the parcel demand they generate, and the pedestrian network connecting them to possible candidate ports. Population data is provided at the household level by the Bergen municipality for the year 2020. Parcel demand is based on PostNord's weekly volumes by postal code of 2020. Finnaly, the walking network of Bergen is extracted from \ac{osm}. The remainder of this chapter describes these sources in detail and the steps taken to prepare them for use in the models that follow.

\subsection{Walking network of Bergen}
\label{subsec:walking_bergen}

\ac{osm} is first used to retreive the walking graph of Bergen by using the Python package \texttt{OSMnx} \parencite{osmnx} using its \texttt{graph\_from\_place()} function. This package offers the ability to retreive the graph of a specific place (\texttt{place\_name=``Bergen, Norway''}) and network type (\texttt{network\_type=``walk''}). However, during stop checking of the walking distance, it was detected that the network type filter was too narrow as the Google Map dap distances were generally smaller than the calculated distance based on the graph. After investigation, the network filter exluded all bycible lanes, even if they were labed as walkable by \ac{osm}. The filter was therefore changed to allow byciable lanes if they are walkable.

\subsection{Population and Household Data}
\label{subsec:population-household-data}

The household data was obtained from the Master' thesis of \cite{pop_thesis} and originaly comes from the Bergen municipality. The dataset, from 2020, has information about population of the city at the household level, only the coordinates, number of resident, postal code and district was kept from the original data. A total of \num{149308} household was contained in the dataset (\num{73393} being unique), with around \num{280491} total population. This demographic is splitted into Bergen' eight districts, and \num{142} unique postal codes.

To reduce the overall values of our data, the household coordinate were snapped to the nearest \ac{osm} node from our walking network. This helped us reduced drasticly the amount of rows for our dataset to \num{29991} unique \ac{osm} nodes. During this aggregation, some \ac{osm} nodes were assigned population that were from multiple differents districts, representing around \num{418} people (\qty{0.15}{\percent} of the population) and \num{27} nodes (\qty{0.07}{\percent} of nodes). Due to some results being shown per district, these nodes were excluded. The population distribution by districts can be seen in \Cref{fig:populations}. It should be noted that some districts will be filtered out of the population data in Section~\ref{ssec:walking_time} (\nameref{ssec:walking_time}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{graphs/district_population.pdf}
  \caption{Total population of Bergen in 2020 aggregated by district.}
  \label{fig:populations}
\end{figure}

Since the goal of this data is to identify good port candidates based on population coverage rather than absolute population size, the data will not be updated to current figures. Only the spatial distribution will be taken into account, and it is assumed that the population growth has been geographically uniform.

\subsection{Candidate Ports}
\label{subsec:candidate-port-locations}

To retrive potential ports location for the Thesis, \texttt{OSMnx} was again used. The \texttt{features\_from\_place()} function offers the positiblty to filters potential features by \ac{osm} tags. Table~\ref{tab:osm_port_tags} shows the filter tags used to retrives ports candiates based on the \ac{osm} documentation \parencite{osm_map_features}.

\begin{table}[htbp]
  \centering
  \caption{OpenStreetMap tags used to extract candiate ports}
  \label{tab:osm_port_tags}

  \begin{tabular}{l l p{8.5cm}}
    \toprule
    \textbf{Key} & \textbf{Value}  & \textbf{Description}                                          \\
    \midrule
    amenity      & ferry\_terminal & Ferry terminal/stop.                                          \\
    man\_made    & pier            & Raised walkway over water.                                    \\
    man\_made    & quay            & Structure alongside navigable water used for loading vessels. \\
    \bottomrule
  \end{tabular}
\end{table}

The resulted data contains \num{746} port locations. The distribution of the ports by their tag used in Table~\ref{tab:osm_port_tags} can be found in Table
\ref{anx:port_nodes_by_tag} of the \nameref{sec:appendix}. As previously done with the household coordinate, the ports location were snaped to the nearest \ac{osm} node on the walking graph. This resulted in a reduction to \num{390} total candiate port nodes. These locations were then projected on a map, and filtered by hand to remove locations that were not suited for this Thesis. For exemple, some node where in a body of water that was not accessble by the main waterways. A total of \num{80} nodes was removed, for a final total of \num{310} port nodes.

\subsection{Distance and Travel Time Data}
\label{subsec:distance-travel-time-data}

Having defined our household nodes and ports nodes datasets, we can now build our distance matrix. To acheive this, the Python package \texttt{networkx} \parencite{networkx} was used to calculate the shortes distance between all combination of port and household nodes using its \texttt{single\_source\_dijkstra\_path\_length()} function. Resulted in a \(m \times n\) matrix \(D\). With \(d_{i,j}\) representing the Dijkstra shortest-path distance between the \(i\)-th port and the \(j\)-th household nodes\footnote{All pairs were reachable by walking due to the \ac{osm} node snapping and filtering of the port nodes.}. For this thesis, the distance between any \(port_i\) and \(household_j\) is assumed to be symmetric (\(d_{i,j} = d_{j,i}\)).

To calculate walking time, an average walking speed first needed to be defined. Based on the work of \cite{walkingspeed}, which reviewed numerous walking speed studies worldwide, a walking speed of \qty{1.25}{m/s} was identified as an appropriate general assumption. However, in the absence of studies specifically focused on Bergen, walking speed may differ from this general assumption and could be significantly affected by seasonal, weather, and topographical conditions, particularly elevation changes along walking paths. As the impact of these factors on walking speed is difficult to quantify specifically, the general assumption will be retained for this thesis. The resulted population coverage by walking time can be seen in Figure~\ref{fig:population_coverage} when all candidate port nodes are available.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{graphs/walking_time_coverage.pdf}
  \caption{Percentage of the population covered by walking time with all ports available.}
  \label{fig:population_coverage}
\end{figure}

\subsection{PostNord Parcel Demand Data}
\label{subsec:postnord-parcel-demand-data}

Parcel demand data was obtained from \cite{household}, but originated from PostNord. It covers weekly (from week \num{1} to \num{48}) shipment and parcel volumes by postal code for the year 2020. The dataset had demand for \num{439} unique postal codes. The given dataset contained information about the number of shipment and packages, however, in many cases, the amount of shipment was larger than the amount of packages for row. The demand was therefore calculated as the minimum of both colums.

As compared to the household data, the real demand size is important to optimize our ports capacity. The PostNord dataset being from 2020, it needs to be adjusted to reflect 2026 levels. This was acheived by using PostNord's annual reports, wich gives the total amount of parcel deliveries for the report's year. The yearly PostNord's demand can be found in Table~\ref{anx:yearly_pn_demand} in the \nameref{sec:appendix}. Based on these yearly demands, the 2026 Bergen demand can be derived using the \ac{cagr} formula. Due to the Covid-19 pandemic, only the years 2022 to 2025 are used to calculate the growth rate, starting from a year in which demand had begun to normalize after the pandemic \parencite{pitney_bowes_index_2023}. As shown in Equation~\ref{eq:postnord_cagr}, the rate is \qty{4.68}{\percent} per year. Therefore, we can approximate that the 2026 demand has grew by around \qty{48.56}{\percent} since 2020.

Second, as the dataset represents only PostNord' share of the market and the year 2020, it needs to be adjusted to the current market figures. Using the \textit{Report on the Use of Postal Services by Private Individuals and Businesses in Norway, 2025}\footnote{Translated from \textit{Rapport om privatpersoners og bedrifters bruk av posttjenester i Norge - 2025} in norwegian.} \parencite{nkom_post_2025} done by the Norwegian Communications Authority, while not provide exact volume-based market shares for parcel deliveries, surveys consumer usage patterns across Norway's primary delivery companies. The market share of PostNord was then derived from the normalized results of the private market parcel survey to be around \qty{20}{\percent}. The survey data can be found in Table~\ref{anx:private_parcel_usage} in the \nameref{sec:appendix}.

Third, this dataset represents parcel demand for both households and other types of clients. However, since this thesis focuses on populations, the non-household demand needs to be removed. To achieve this, the household postcode data was joined with the weekly demand, and postcodes not present in the household dataset were excluded.

Finally, since some districts will be filtered out of the population data in Section~\ref{ssec:walking_time} (\nameref{ssec:walking_time}), the demand associated with these excluded household districts will also be removed. The overall demand quantities and changes by step can be seen in Table~\ref{anx:parcel_volumes} in the \nameref{sec:appendix}. The total parcel demand taken in considuration for this Thesis will be \num{5977449} parcel for the year 2026.

\subsection{Disaggregating Weekly Demand into Daily Demand}
\label{subsec:weekly-to_daily-demand}

Since daily scheduling for the boat network requires daily demand for each selected port, the PostNord weekly demand needs to be broken down. While several approaches were possible, such as splitting the demand equally across days or distributing it randomly, applying specific daily rates was chosen as the most representative of real demand patterns. Based on the work of \cite{dailydemand}, where a daily operational parcels operations was studied, the 5 Day split for this Thesis context can therefore be deduce by adding the weekend's demand to Monday. The resulted daily rates can be seen in Table~\ref{anx:day_shares} in the \nameref{sec:appendix}.

\subsection{Disaggregating Postcal Code Demand into Household Demand}
\label{subsec:postal-code-to-household-demand}

To correcly assinged demands to the nearest chosen ports, the demand of each Households must be known. However, the PostNord dataset is only by postal code. Nevertheless, the demand can still be allocated to each household based on its share of the population within its postal code. For example, if a postal code has a weekly demand of 100 parcels and contains two households, one with 1 resident (\(a\)) and the other with 2 (\(b\)), the demand is distributed as \(\frac{1}{3}\) to \(a\) and \(\frac{2}{3}\) to \(b\). In addition, each household demand was rounded to have integer demand, resulting in a loss of \num{15.69} total parcels or \qty{0.000263}{\percent} of total demand.

\subsection{Depot Location}
\label{sub:depot_loc}

With the candidate ports and their demand defined, the depot location can now be chosen. The depot is where the incoming parcels are received, processed and loaded onto the boats, so it needs enough space for this work and for docking the boats. However, these processing and handling spaces will be taken into account as context, but not quantified in this thesis. Furthermore, a good depot location should be close to the Port of Bergen and its cargo rail station, as both are the second largest of their kind in Norway \parencite{bergenport,bergenrail}. Finally, the depot also acts as a port itself, so a location with a high population nearby is the most efficient choice, as it removes the most parcels from boat transport.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.4\textwidth]{density_focused.pdf}
  \caption{Map of Bergen city center showing household density in 2020 and the locations of candidate ports relative to the (A) Port of Bergen and the (B) train station.}
  \label{fig:full_density}
\end{figure}

The first step consists of identifying the locations of the Port of Bergen and the railway station, and overlaying them with household density. As shown in Figure~\ref{fig:full_density}, the two facilities are spatially separated. The port is located along the shoreline, whereas the railway station is situated further inland. Since the depot also needs to be close to the shore, the range of potential locations becomes considerably restricted. A full map density can be fun in Figure~\ref{anx:map_density} of the \nameref{sec:appendix}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.4\textwidth]{maps/depot_map.pdf}
  \caption{Map of the selected depot location.}
  \label{fig:depot_map}
\end{figure}

The location that was chosen can be seen in Figure~\ref{fig:depot_map}, as it is directly in a high density area, close to the port and relativly near the rail station. Additionally, it offers some empty space near the shore. As explained earlier, this space may not be actually usable for this project, but as for the candidate ports, it will be assumed that all locations offers enought space and could be used to build on.

\subsection{Operational Constraints}
\label{ssec:operational_constraints}

The models presented later in this thesis are based on a set of constraints that must be defined and justified before they can be implemented. The following section details each constraint and explains the values assigned to them. Some operational constraints, such as handling time at the depot or travel time between ports and lockers, are excluded from the scope of this thesis. Including these constraints would require a more detailed specification of the operational process and more precise port-to-locker distance data. Despite these exclusions, the selected constraints provide an adequate foundation for the development of the models.

\subsubsection{Boat Maximum Capacity and Speed}
\label{subsubsec:maximum_capacity}

This thesis will use three scenarios to simulate different types of boats used in this network, each representing a different total parcel capacity, from a low to a hight one.

Low-capacity scenario will be based on the Candela P-12 Business boat \parencite{candela2026p12}. Even if this boat is not meant for cargo transportation, it will be used as a representative vessel class to provide realistic characteristics for the models. Its service speed of \qty{25}{knots}, \num{30}-passenger capacity, and total payload limit of \qty{3000}{\kilogram} are taken directly from the manufacturer's specifications \parencite{candela2026p12}. To determine the parcel capacity, the passenger weight limit is used as a baseline. Assuming a standard passenger weight of \qty{75}{\kilogram} \parencite{passengerkg}, the payload dedicated strictly to the parcels is \qty{2250}{\kilogram}. With an average parcel weight of \qty{4.5}{\kilogram}, this yields a total capacity of \num{500} parcels. The remaining \qty{750}{\kilogram} of the boat's total \qty{3000}{\kilogram} limit is then allocated to the handling materials required for these parcels. Dividing this remaining capacity by the \num{500} parcels yields a handling material weight of exactly \qty{1.5}{\kilogram} per parcel. This \qty{1.5}{\kilogram} per parcel ratio will be used across the other scenarios to maintain a consistent proportion of handling weight to loading capacity.

Mid-capacity scenario will be based on the Kewatec Work 1920 \parencite{kewatec_work_1920}, a larger vessel that resembles more of a barge and is made to transport cargo of all sorts. The total load capacity can therefore be taken directly as \qty{15000}{\kilogram} and for a cruising speed of \qtyrange{25}{35}{knots}. With this capacity, the same average parcel weight of \qty{4.5}{\kilogram} per parcel, and the \qty{1.5}{\kilogram} per parcel for handling, the total parcel capacity can be derived to exactly \num{2500} parcels.

High-Capacity scenario is based on the Kewatec Work 1500 \parencite{kewatec_work_1500}, a vessel whose physical size sits directly between the small and large boat choices. Because specific payload data is not published by the manufacturer, a capacity of \qty{9000}{\kilogram} is assumed to create a clean middle ground between the other two scenarios. When applying the established parcel unit weight of \qty{6.0}{\kilogram}, this payload capacity yields a total limit of exactly \num{1500} parcels with again a speed of \qtyrange{25}{35}{knots}.

For all scenarios, the range of the boats will not be taken into considuration for this thesis, as the route are not real waterway obptimised routes, and it is taken for granted that the boats could stop to reafuel or place a strategic elevtric charger at a hight demand ports and depot to recharfe while unloading the boat.

\subsubsection{Crew Size}
\label{subsubsec:crew_size}

Each boat is operated by a crew. The crew is responsible for discharging the parcels at the ports, and since the employees discharge parcels in parallel, the discharge time at a port is divided by the number of employees on board. A larger crew therefore lowers the time spent at each port, but it also raises the number of employees the service needs. For this reason, the crew size is also treated as a parameter and tested with \num{3} and \num{4} employees per boat.

\subsubsection{Maneuvering Time}
\label{subsubsec:maneuvering_time}

Every time the boat travels from one port to another, it needs time to dock and undock. This thesis accounts for it with a fixed maneuvering time, added to the travel time of every trip between two ports. The value used is a baseline of \qty{3.5}{\minute}, taken as the average of two estimates: a conservative one of about \qty{5}{\minute}, derived from a study of port operations on a passenger ferry \parencite{klovning2024efficiency}, and an optimistic one of \qty{2}{\minute}, based on the Candela P-12 \parencite{candela2026p12}.

\subsubsection{Maximum Route Duration}
\label{subsubsec:maximum_route_duration}

A single boat route, from the moment it leaves the depot to the moment it returns, cannot last longer than \qty{8}{\hour}. This upper bound represents a normal working shift for the crew. The duration of a route is the sum of all its travel times, including the maneuvering time, and the discharge time at every port it visits. Every route built by the routing models has to respect this limit.

\subsubsection{Discharge Time}
\label{subsubsec:discharge_time}

The discharge time is the time needed to handle one parcel once the boat has docked. This thesis uses a fixed value of \qty{25}{\second} per parcel \parencite{toiparcellockers}. The total discharge time at a port is then the number of parcels delivered there, multiplied by \qty{25}{\second} and divided by the crew size, since the employees on board discharge the parcels in parallel. Together with the travel and maneuvering times, this discharge time makes up the total duration of a route.

\subsubsection{Operating hours}
\label{subsubsec:operating_hours}

To schedule the operations correctly, the operating hours must first be defined. Since this thesis focuses on Bergen, Norway, information from parcel service providers operating in the region, such as PostNord and Posten, can be used as a reference. Both providers offer parcel delivery services on weekdays, from Monday to Friday, with some exceptions made by Posten for certain types of parcels on Saturdays \parencite{posten_hours,portnord_hours}. Therefore, the operating days in this thesis are also set from Monday to Friday.

In addition, the daily operating hours must be specified. In this case, the Norwegian Working Environment Act provides a useful reference point. According to the Act, \enquote{Ordinary working hours must not exceed nine hours in a 24-hour period} \cite{arbeidsmiljoloven_10_4}. However, since this represents the maximum legal limit, a standard maximum of \qty{8}{\hour} per employee is assumed in this thesis. The overall operation is nevertheless modeled over a \qty{24}{\hour} planning horizon, allowing several employees or shifts to cover the full day if required.

\subsection{Data Limitations}
\label{subsec:data-limitations}

Although a substantial amount of data is available to address the thesis's research problem, some limitations remain: part of the required information is missing, and other elements must be approximated. The following subsections discuss each of these limitations in turn.

\subsubsection{Lack of data}

The first data that is missing is the information about the weight and volumn of the parcels, only the demand is present. Even if some average volumn and weight was chosen for the demand, the boat transporting the packages does not have any real volumn or weight limits, only a capacity was chosen. Therefore, these two variables will be ignored. The second data that is missing is related to the ports candiate real hability to be converted to a port location. Indeed, some ports could be not suitable for this operation or be privite and in no way of having a locker system be build on it.

\subsubsection{Approximation}

As described in the previous subsections, some data had to be approximated since exact figures were not available. First, the weekly demand was disaggregated into daily demand, and then further broken down to the household level. The resulting demand was subsequently scaled to the current year and finally adjusted to reflect the total market share of all shipping companies. The thesis could have directly used the 2020 data, but this would have produced an outdated scenario for waterway parcel deliveries, since demand has grown significantly since then.

Furthermore, walking time is directly computed from the walking distance and does not account for elevation changes. This simplification is consistent with common practice in pedestrian network analysis, as similar frameworks do not dynamically account for topographic slopes \parencite{noslope}. While elevation can have a significant impact on pedestrian routing, especially in hilly cities \parencite{slopegood}, incorporating slope-based travel time falls outside the scope of this thesis as it would need to take into account the total affect on the round trip to the port and is identified as a direction for future work.

\section{Methodology}
\label{sec:methodology}

In this section, the methodology of this thesis is explained in detail, in four separate parts. First, the model used to select a set of ports based on a desired walking distance is presented. Second, the two models used to generate a set of routes for these ports are described. Finally, it is explained how, given a set of ports and routes, a schedule can be constructed to measure the KPIs based on the yearly demand.

\subsection{Port Selection Model}
\label{subsec:port_selection_model}

The goal of this stage is to find the smallest set of ports that still covers a target share of the population within an acceptable walking time. The selection depends on two inputs: the walking threshold and the target coverage percentage. These two configurations are presented first, followed by the optimisation model chosen to perform the selection.

\subsubsection{Walking Thresholds}
\label{ssec:walking_time}

\begin{figure}[httbp]
  \centering
  \includegraphics[width=1\textwidth]{graphs/walking_time_coverage_district.pdf}
  \caption{Percentage of the population covered by walking time with all ports available by district.}
  \label{fig:district_coverage}
\end{figure}

As discussed in the background section, a walking time of \qty{15}{minutes} is often the new goal when planning new city programs for its population. The walking time coverage distribution was alreay shown in Figure~\ref{fig:population_coverage}, but it can be important to show also see the same data by district. This can be visualize in Figure~\ref{fig:district_coverage}.

% ['Arna', 'Bergenhus', 'Fana', 'Fylingsdalen', 'Laksevåg', 'Ytrebygda', 'Årstad', 'Åsane']

The districts can be visually grouped into three categories based on how their population coverage evolves with the walking time threshold. The first group, composed of Bergenhus ({\color[HTML]{FF7500}\rule{1.5ex}{1.5ex}}), Ytrebygda ({\color[HTML]{7F7F7F}\rule{1.5ex}{1.5ex}}), and Laksevåg ({\color[HTML]{955348}\rule{1.5ex}{1.5ex}}), shows a rapid increase in coverage, reaching near-full coverage within approximately \qtyrange{35}{40}{\minute} of walking. The third group, formed by Arna ({\color[HTML]{0079B9}\rule{1.5ex}{1.5ex}}), Fana ({\color[HTML]{00A300}\rule{1.5ex}{1.5ex}}), and Åsane ({\color[HTML]{00C1D2}\rule{1.5ex}{1.5ex}}), are the opposite: coverage grows slowly and does not reach \qty{90}{\percent} even at a one-hour walking threshold, plateauing between \qtyrange{75}{85}{\percent}. The second group, composed of Årstad ({\color[HTML]{BCBD00}\rule{1.5ex}{1.5ex}}) and Fyllingsdalen ({\color[HTML]{9C65C2}\rule{1.5ex}{1.5ex}}), follows an intermediate pattern: their coverage initially grows more slowly than in the first group, but eventually reaches \qty{100}{\percent} at higher walking thresholds, around \qtyrange{50}{55}{\minute}. With these remarks, it was decided to exclude Arna, Fana and Åsane from the thesis as our port location can not reach them in a realistic time, combine with the points talked in the backtround section.

The walking time threshold considered for this problem ranges from \qtyrange{15}{30}{\minute}, with every integer value within this range tested. Values below \qty{15}{\minute} were judged as too low for this context, as the resulting coverage was very limited, while values above \qty{30}{\minute} were judged as impractical, since citizens are unlikely to walk such distances to collect a parcel.

\subsubsection{Coverage Percentages}
\label{ssec:coverage_per}

As achieving \qty{100}{\percent} coverage for a given walking time threshold could require a disproportionately large number of boats, due to certain households only being reachable through specific ports, a minimum coverage percentage will instead be defined to address this issue. It was decided that the range would be \qtyrange{75}{100}{\percent}, with every percentage point within this range tested. This range should be large enough to show if some ports are added only for a small portion of the population.

\subsubsection{Partial Set Covering Location Problem}
\label{ssec:pscl_problem}

Although various models exist to address this routing problem, the \ac{psclp} was selected for this thesis. It perfectly matches the network's operational objectives as it minimizes the number of active ports while relaxing the requirement to reach all households \parencite{psclp}.

Table~\ref{tab:pslcp_configs} presents the model configurations, all combinations of which were tested.

\begin{table}[htbp]
  \centering
  \caption{Configurations for the \ac{psclp}}
  \label{tab:pslcp_configs}

  \begin{tabularx}{\textwidth}{c X l}
    \toprule
    \textbf{Symbol} & \textbf{Description}                           & \textbf{Value}            \\
    \midrule
    \(\alpha\)      & Target demand coverage threshold               & \{0.75, 0.76, \ldots, 1\} \\
    \(T_{\max}\)    & Maximum allowable walking time limit (minutes) & \{15, 16, \ldots, 30\}    \\
    \bottomrule
  \end{tabularx}
\end{table}

Most of the model implementation comes directly from the \cite{psclp} paper, altougt some changes were made to make it fit with this thesis. Table~\ref{tab:psclp_notation} details the notation used in the model. The changes made in the notation were mostly done to contextualize it for the thesis, as we have household and port nodes instead of demand nodes and facilities, while using population instead of weight. The \textit{Data and Sets} section of the table describes what the model needs as input, with the set of port and household nodes and population for each households. The reachable household nodes \(J\) is calculated using the previously defined distance matrix \(M\), where every household nodes whitout a port node close enough is removed from considuration. The maximum distance used is derived from the walking speed and the current run \(T_{max}\) resulted in a maximum amount of meters to given to reach a possible port.

\begin{table}[htbp]
  \centering
  \caption{Notation for the \ac{psclp}}
  \label{tab:psclp_notation}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{c X l}
    \toprule

    \multicolumn{3}{l}{\textbf{\textit{Data and Sets}}}                                                                                                  \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                                               \\
    \midrule
    \(I\)           & \multicolumn{2}{l}{Set of candidate port nodes, indexed by \(i\)}                                                                  \\
    \(J\)           & \multicolumn{2}{l}{Set of reachable household nodes, indexed by \(j\)}                                                             \\
    \(d_j\)         & \multicolumn{2}{l}{Household population associated with node \(j\)}                                                                \\
    \(I(j)\)        & \multicolumn{2}{l}{Subset of ports \(i \in I\) capable of reaching household node \(j\) in \(T_{\max}\)}                           \\
    \(P\)           & \multicolumn{2}{l}{Subset of preselected ports that must be opened, currently the depot}                                           \\
    \midrule

    \multicolumn{3}{l}{\textbf{\textit{Parameters}}}                                                                                                     \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                     & \textbf{Value}          \\
    \midrule
    \(D\)           & Minimum total household population to be covered                                                         & \(\alpha \sum d_j\)     \\
    \(f_i\)         & Fixed cost of opening port \(i\)                                                                         & 1                       \\
    \midrule

    \multicolumn{3}{l}{\textbf{\textit{Decision Variables}}}                                                                                             \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                     & \textbf{Value}          \\
    \midrule
    \(y_i\)         & \(\begin{cases} 1, & \text{if port } i \text{ is opened} \\ 0, & \text{otherwise} \end{cases}\)          & Binary \(\in \{0, 1\}\) \\
    [3ex]
    \(z_j\)         & \(\begin{cases} 1, & \text{if demand node } j \text{ is covered} \\ 0, & \text{otherwise} \end{cases}\)  & Binary \(\in \{0, 1\}\) \\
    \bottomrule
  \end{tabularx}
\end{table}

Building upon the established notation, the problem is formulated as an optimization model as follows:

\begin{align}
  \min \quad & \sum_{i \in I} f_i y_i \label{eq:psclp-objective}                                        \\
  \text{s.t.} \quad
             & \sum_{i \in I(j)} y_i \geq z_j
             &                                                   & j \in J \label{eq:psclp-coverage}    \\
             & \sum_{j \in J} d_j z_j \geq D
  \label{eq:psclp-demand}                                                                               \\
             & y_i = 1
             &                                                   & i \in P \label{eq:psclp-preselected} \\
             & y_i \in \{0,1\}
             &                                                   & i \in I \label{eq:psclp-y-binary}    \\
             & z_j \in \{0,1\}
             &                                                   & j \in J \label{eq:psclp-z-binary}
\end{align}

The objective function~\eqref{eq:psclp-objective} traditionally minimizes the total fixed cost of the opened ports. Since every candidate port is given the same opening cost (\(f_i = 1\)), this is the same as minimizing the number of ports selected for the network.

Constraint~\eqref{eq:psclp-coverage} ensures that for a household node \(j\) to be covered (\(z_j = 1\)), at least one port from its subset \(I(j)\) must be opened. If no port able to reach \(j\) within the walking threshold \(T_{\max}\) is opened, \(z_j\) is forced to bo not covered (\(z_j = 0\)).

Constraint~\eqref{eq:psclp-demand} ensures that the covered set of household populations is greater or equal to our minum covered population \(D\).

Constraint~\eqref{eq:psclp-preselected} forces every preselected port to be opened. The depot is part of this set, as the boats operate out of it and it must therefore always be among the selected ports (Section~\ref{sub:depot_loc}).

The last two constraints,~\eqref{eq:psclp-y-binary} and~\eqref{eq:psclp-z-binary}, keep the port and coverage variables binary.

\subsubsection{Model implementation}
\label{ssec:psclp_implementation}

The \ac{psclp} is implemented with Google OR-Tools \parencite{ortools}, using its constraint-programming solver CP-SAT \parencite{cpsat}. One adjustment was needed to fit the model to the solver. CP-SAT only works with integer coefficients, but the coverage target \(D = \alpha \sum_{j \in J} d_j\) is usually not a whole number, since \(\alpha\) is a fraction. To account for this, the population and the coverage target are multiplied by \num{1000} and rounded to integers before the demand constraint~\eqref{eq:psclp-demand} is built. This keeps three decimal places of precision, so the rounding has no real effect on the result.

The model is kept as compact as possible by removing, before optimization, all households that cannot be covered by any candidate port within the walking threshold \(T_{\max}\). These households are therefore excluded from the formulation, and no associated coverage variable \(z_j\) is introduced.

\subsubsection{Scenario Selection}
\label{ssec:scenario_selection}

Each pair \((T_{\max}, \alpha)\) produces one feasible \ac{psclp} solution, but carrying every combination through the routing and scheduling steps would be impractical. A subset is therefore selected using the \textit{utopia point} method: the coverage \(\alpha\), the number of opened ports and the average weighted walking time are min-max normalised across the relevant set of runs, and the run with the shortest Euclidean distance to the ideal \((1, 0, 0)\) point (maximum coverage, minimum ports, minimum walking time) is retained.

This rule is applied twice. First, for each walking threshold \(T_{\max}\), the best run among the feasible \(\alpha\) values is kept; the run at \(T_{\max} = \qty{15}{\minute}\) is of particular interest as it matches the ``15-minute city'' reference discussed in the background section. Second, a single overall best run is identified across all \((T_{\max}, \alpha)\) pairs. These selected scenarios are then carried forward to the boat routing models presented in the next section.

\subsection{Boat Routing Models}
\label{subsec:boat_routing_models}

With the selected ports and their daily demand scenarios in hand, the next step is to design the boat routes that serve them. The routing is formulated as a \ac{scvrp}, which is fast to solve and well studied in the literature. To handle the ports whose demand approaches or exceeds the capacity of a single boat, the formulation is paired with a service-node decomposition that emulates split deliveries while keeping the standard \ac{scvrp} structure.

\subsubsection{Demand Allocation to Selected Ports}
\label{ssec:demand_allocation}

The \ac{psclp} gives a set of selected ports, but it does not say the households each port should serve. Each household is assigned to the nearest selected port based on the distance matrix \(M\). The total household demands is then summed by ports to get a weekly demand for each ports.

It should be noted that this is done for every household, not only for the ones the \ac{psclp} counted as covered. A household that was left uncovered, meaning that no selected port can be reached within the walking threshold \(T_{\max}\), is still assigned to its nearest selected port, only with a longer walk. The selected ports therefore receive the parcel demand of every household in the included districts, and the coverage threshold \(\alpha\) only shapes which ports are selected and how far people have to walk, not the amount of demand the network has to carry.

\subsubsection{Ports Distance matrix}
\label{ssec:ports_distance_matrix}

Although the computation differs from the household-to-port distance matrix, the same matrix-based logic is used here. A separate port-to-port distance matrix is constructed to represent the travel distances between each pair of selected ports. However, in complex environments like the fjords in Bergen, calculating true maritime distances between close ports is difficult. Standard Euclidean distances are inadequate as linking two ports with a straight line will probobly go straight trought the montain, ignoring any complexe and small waterway of the real boat route.

Sophisticated algorithmic solutions exist to navigate around such spatial constraints, typically based on visibility graphs or shortest-path methods built on top of the coastline polygons. Building and maintaining such a graph for Bergen's full coastline is however non-trivial and falls outside the scope of this thesis, which does not aim to produce strict navigation guidelines or the exact best route between two ports, but rather a network-level estimate of boat travel time. Given the relatively small number of ports retained by the \ac{psclp}, a simpler manual approach is used instead and is precise enough for the routing models that follow.

The construction is done in two steps. The first step is done manually in QGIS. The selected ports are first grouped into a small number of visual zones based on their geographic emplacement, with one zone per coherent area of the coastline (for example the inner fjord around the depot, the northern shore, etc.). The relevant distances are then drawn directly on top of the \ac{osm} basemap as polylines that follow a plausible boat path between two ports, and the length of each polyline is taken as the distance. Three families of arcs are drawn: every pair of ports within the same zone, a single edge-to-edge polyline between each pair of zone polygons, and the polyline between every port and the depot. The choice of restricting inter-zone measurements to the polygon edges comes from computational geometry: for two convex regions, the shortest path between them always touches a point on their boundary, never their interior, so the edge-to-edge polyline is enough to represent the inter-zone connection. The slow-speed segments at \qty{5}{knots} and \qty{8}{knots} introduced in the background section are also captured during this step, by tagging the portion of each polyline that crosses the corresponding restricted zones.

The second step is done in code and fills in the arcs that were not drawn by hand. The directly measured arcs from the first step are loaded as the edges of a weighted graph over the selected ports, with edge weights equal to the polyline lengths. For every port pair that has no direct measurement, the shortest path through the graph is computed with Dijkstra's algorithm and used as an estimate of the missing distance. An overhead correction is applied to each intermediate stop on the path to account for the fact that a boat going directly between two ports would not have to detour through every port on the way. The output is a complete port-to-port distance matrix, combining the directly measured arcs from the first step and the graph-based estimates from the second one, which is the matrix consumed by the routing models.

\subsubsection{Demand Scenarios}
\label{ssec:demand_scenarios}

The routing models do not run on the raw daily port demand, but on three representative daily demand scenarios per port: a Low, an Average and a Peak scenario. Working with discrete scenarios rather than the full daily series keeps the number of routing runs manageable while still covering a quiet, a typical and a busy day, which is enough to test whether the fleet sized for an average day can absorb seasonal swings.

The three scenarios are anchored on three reference months. For every month of the year, the median of the network's total daily demand is computed, and the months are ranked by this median. The month with the lowest median is taken as the \textit{low month}, the one in the middle of the ranking as the \textit{average month}, and the one with the highest median as the \textit{peak month}. Using months instead of individual days smooths out single-day noise, like holidays or weather events, while keeping the seasonal signal.

Once the three reference months are fixed, each scenario is built at the port level. The Low scenario takes, for each port, the median of its daily demand observed during the low month, which corresponds to a typical quiet day. The Average scenario does the same on the average month and represents a typical day on which the network should comfortably operate. The Peak scenario takes the 95\textsuperscript{th} percentile of the port's daily demand observed during the peak month. The 95\textsuperscript{th} percentile is preferred to the maximum to represent a realistically busy day without sizing the fleet on a single extreme outlier. Each scenario value is rounded up to the next integer so that the routing models treat demand as whole parcels.

\subsubsection{Model implementation}
\label{ssec:scvrp}

The first routing model used is the \ac{scvrp} as it is a natural fit for this problem: the depot is fixed, each selected port has a known weekly parcel demand, and the fleet is composed of identical boats with a maximum parcel capacity and a maximum daily working time. The objective is to design a set of routes, each starting and ending at the depot, that together serve all port demands while minimising the total travel time of the fleet.

Several configurations were tested for the \ac{scvrp}, each combining a boat capacity, a crew size and a weekly demand scenario, summarised in Table~\ref{tab:cvrp_configs}. The three boat scenarios and their parcel capacity are defined in Section~\ref{subsubsec:maximum_capacity}, the two crew sizes in Section~\ref{subsubsec:crew_size}, and the three demand scenarios in Section~\ref{subsec:port-demand-results}. The boat cruising speed is fixed across all configurations at \qty{25}{knots}, which is the common reachable speed of every vessel considered in Section~\ref{subsubsec:maximum_capacity}.

\begin{table}[htbp]
  \centering
  \caption{Configurations tested for the \ac{scvrp}}
  \label{tab:cvrp_configs}

  \begin{tabularx}{\textwidth}{c >{\raggedright\arraybackslash}X l}
    \toprule
    \textbf{Symbol} & \textbf{Description}   & \textbf{Values}                               \\
    \midrule
    \(Q\)           & Boat parcel capacity   & \{Low-capacity, Mid-capacity, High-capacity\} \\
    \(e\)           & Employees per boat     & \{2, 3, 4\}                                   \\
    \(s\)           & Weekly demand scenario & \{Low, Average, Peak\}                        \\
    \bottomrule
  \end{tabularx}
\end{table}

The classic \ac{scvrp} mathematical model is not defined in this thesis, as it was also not explicitly defined in the code. The OR-Tools routing library \parencite{ortools} handles a lot of the standard formulation under the hood, like the routing decision variables, the flow conservation and the sub-tour elimination. What follows first describes the objective and the constraints layered on top of the classic model, then the solver and search parameters used to solve it. Table~\ref{tab:scvrp_notation} summarises the additional notation used in the rest of this section.

\begin{table}[htbp]
  \centering
  \caption{Notation used in the \ac{scvrp} model}
  \label{tab:scvrp_notation}

  \begin{tabularx}{\textwidth}{c X l}
    \toprule
    \textbf{Symbol}  & \textbf{Description}                           & \textbf{Value}                                                    \\
    \midrule
    \(t_{ij}\)       & Boat travel time between ports \(i\) and \(j\) & From port distance matrix                                         \\
    \(m\)            & Manoeuvring time per arc                       & \qty{3.5}{\minute} (See Section~\ref{subsubsec:maneuvering_time}) \\
    \(\tau\)         & Discharge time per parcel                      & \qty{25}{\second}  (See Section~\ref{subsubsec:discharge_time})   \\
    \(d_i\)          & Parcel demand at port \(i\)                    & From demand matrix                                                \\
    \(T^{r}_{\max}\) & Maximum route duration                         & \qty{8}{\hour}  (See Section~\ref{subsubsec:operating_hours})     \\
    \bottomrule
  \end{tabularx}
\end{table}

The objective minimises the total travel time of the fleet, where the cost of every arc is the sum of the boat travel time \(t_{ij}\) between the two ports and the manoeuvring time \(m\). The parcel handling time is left out of the optimization function as it is a constant, all of the demand will be handled at the same speed regardless. To allow split deliveries while keeping the standard \ac{scvrp} structure, where each stop is visited by exactly one vehicle, the demand of every port is decomposed into multiple service nodes that share the port's coordinates, each one carrying at most \num{10} parcels. The routing model treats these service nodes as independent stops but with no travel cost in-between them (no distance), making possible it for the same port can be visited several times by different routes.

The handling time at port \(i\) is \(d_i \tau / e\), with \(d_i\) the parcel demand of the port for the run, \(\tau\) the per-parcel discharge time from Section~\ref{subsubsec:discharge_time}, and \(e\) the crew size of the configuration. It represents an estimation of the time it will take to put the parcels into the lockers at the port. As mentionned above, the travel time between the port and the locker will not be taken into account in this thesis.

Two capacity constraints are added to the solver. The first corresponds to the maximum parcel capacity of the boat in the current configuration, denoted by \(Q\). This constraint ensures that the total demand assigned to a route does not exceed the boat capacity. The total demand is calculated by summing the demand covered by the boat at each visited port and is calculated by summing all theses times categories for each ports visied in the current route.

The second constraint corresponds to the maximum route duration, denoted by \(T^{r}{\max}\). This constraint represents an operational restriction requiring employees to complete their route before the end of their shift. The total route duration includes travel time, maneuvering time, and handling time at each visited port.

The search is configured with \texttt{PARALLEL\_CHEAPEST\_INSERTION} as the first-solution strategy and \texttt{GUIDED\_LOCAL\_SEARCH} as the local-search metaheuristic \parencite{ortools_routing}, with a time limit of \qty{15}{\minute} per run. This combination produces a feasible solution quickly and then iteratively improves it; the best solution found within the time limit is retained.

\subsubsection{Boat Scheduling Optimization Model}
\label{ssec:scheduling}

The \ac{scvrp} gives the set of routes that the fleet needs to run on a given day, but not how many boats and crews are needed to run them. The boat scheduling model fills this gap by assigning each route to a boat and deciding how many crew shifts each boat has to run. The fleet size and the total shift count are what fall out of the solution, and they directly drive the \ac{kpi}s computed in the next section.

A simple bin packing model is enough at this stage. The routes from the \ac{scvrp} already start and end at the depot and respect the maximum route duration, so two routes on the same boat can be run one after the other with no scheduling conflict. The network also does not commit to specific delivery times at the ports, so no time windows have to be enforced. The model therefore only has to decide which routes share a boat and how many shifts each boat needs. The boat count is minimised first and the total shift count second, since the boat count sets the capital cost of the network and the shift count sets the labour cost. A richer model with port time windows or crew rest rules would add more detail than the data of this thesis can support, without changing the fleet size it gives.

Each route is treated as a single fixed-duration, indivisible task equal to its total route time. Boats are interchangeable, and a single boat can run several routes during the day, as long as the total work fits within the \qty{24}{\hour} planning horizon from Section~\ref{subsubsec:operating_hours}. A single crew is however limited to one shift of at most \qty{8}{\hour}, so a boat that works more than eight hours in the day must operate over several shifts, each shift covered by a new crew. Because a route cannot be split across shifts, the model assigns every route to a specific shift of a specific boat, and not only to a boat. Table~\ref{tab:scheduling_notation} summarises the notation used in the formulation.

\begin{table}[htbp]
  \centering
  \caption{Notation for the boat scheduling model}
  \label{tab:scheduling_notation}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{c X l}
    \toprule

    \multicolumn{3}{l}{\textbf{\textit{Data and Sets}}}                                                                                                                           \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                                                                        \\
    \midrule
    \(R\)           & \multicolumn{2}{l}{Set of routes returned by the \ac{scvrp}, indexed by \(r\)}                                                                              \\
    \(K\)           & \multicolumn{2}{l}{Set of available boats, indexed by \(k\), with \(|K| = |R|\)}                                                                            \\
    \(s\)           & \multicolumn{2}{l}{Shift index within a boat, \(s \in \{1, \ldots, \bar{n}\}\)}                                                                             \\
    \(\delta_r\)    & \multicolumn{2}{l}{Total duration of route \(r\)}                                                                                                           \\
    \midrule

    \multicolumn{3}{l}{\textbf{\textit{Parameters}}}                                                                                                                              \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                                              & \textbf{Value}          \\
    \midrule
    \(H\)           & Operating day length                                                                                                              & \qty{24}{\hour}         \\
    \(S\)           & Maximum shift length                                                                                                              & \qty{8}{\hour}          \\
    \(\bar{n}\)     & Maximum number of shifts per boat (\(\lceil H / S \rceil\))                                                                       & 3                       \\
    \(M\)           & Large weight on the boat count in the objective                                                                                   & \num{10000}             \\
    \midrule

    \multicolumn{3}{l}{\textbf{\textit{Decision Variables}}}                                                                                                                      \\
    \midrule
    \textbf{Symbol} & \textbf{Description}                                                                                                              & \textbf{Value}          \\
    \midrule
    \(y_{rks}\)     & \(\begin{cases} 1, & \text{if route } r \text{ is assigned to shift } s \text{ of boat } k \\ 0, & \text{otherwise} \end{cases}\) & Binary \(\in \{0, 1\}\) \\
    [3ex]
    \(u_k\)         & \(\begin{cases} 1, & \text{if boat } k \text{ is used} \\ 0, & \text{otherwise} \end{cases}\)                                     & Binary \(\in \{0, 1\}\) \\
    [3ex]
    \(z_{ks}\)      & \(\begin{cases} 1, & \text{if shift } s \text{ of boat } k \text{ is used} \\ 0, & \text{otherwise} \end{cases}\)                 & Binary \(\in \{0, 1\}\) \\
    \bottomrule
  \end{tabularx}
\end{table}

The problem is formulated as follows:

\begin{align}
  \min \quad & M \sum_{k \in K} u_k + \sum_{k \in K} \sum_{s = 1}^{\bar{n}} z_{ks} \label{eq:sched-objective}                                                                                 \\
  \text{s.t.} \quad
             & \sum_{k \in K} \sum_{s = 1}^{\bar{n}} y_{rks} = 1                                              &  & r \in R \label{eq:sched-assign}                                            \\
             & \sum_{r \in R} \sum_{s = 1}^{\bar{n}} \delta_r \, y_{rks} \leq H \, u_k                        &  & k \in K \label{eq:sched-day}                                               \\
             & \sum_{r \in R} \delta_r \, y_{rks} \leq S \, z_{ks}                                            &  & k \in K,\ s \in \{1, \ldots, \bar{n}\} \label{eq:sched-shift}              \\
             & z_{ks} \leq u_k                                                                                &  & k \in K,\ s \in \{1, \ldots, \bar{n}\} \label{eq:sched-link}               \\
             & u_k \geq u_{k+1}                                                                               &  & k \in K \setminus \{|K|\} \label{eq:sched-symmetry-boat}                   \\
             & z_{ks} \geq z_{k,\,s+1}                                                                        &  & k \in K,\ s \in \{1, \ldots, \bar{n} - 1\} \label{eq:sched-symmetry-shift} \\
             & y_{rks} \in \{0,1\}                                                                            &  & r \in R,\ k \in K,\ s \in \{1, \ldots, \bar{n}\} \label{eq:sched-y-binary} \\
             & u_k \in \{0,1\}                                                                                &  & k \in K \label{eq:sched-u-binary}                                          \\
             & z_{ks} \in \{0,1\}                                                                             &  & k \in K,\ s \in \{1, \ldots, \bar{n}\} \label{eq:sched-z-binary}
\end{align}

The objective function~\eqref{eq:sched-objective} minimises the number of boats used first and the total number of shifts second. The fleet size is prioritised because each additional boat is a capital investment, while a shift only adds a crew for the day. The weight \(M\) on the boat count is set well above the maximum possible total shift count \(|K| \cdot \bar{n}\), so the solver can never trade a shift saving for an extra boat.

Constraint~\eqref{eq:sched-assign} forces every route to be assigned to exactly one (boat, shift) pair.

Constraint~\eqref{eq:sched-day} caps the total workload of boat \(k\) at the operating day length \(H\) when the boat is used, and forces it to zero otherwise.

Constraint~\eqref{eq:sched-shift} is the bin-packing constraint at the heart of the model: the sum of route durations placed in shift \(s\) of boat \(k\) must fit within the maximum shift length \(S\), and the shift is marked as used (\(z_{ks} = 1\)) as soon as any route is assigned to it. This per-shift bound is what guarantees feasibility at the crew level, since routes are indivisible and cannot be split across shifts. A weaker aggregate bound of the form \(\sum_{r, s} \delta_r \, y_{rks} \leq S \, \sum_s z_{ks}\) would for instance allow four \qty{6}{\hour} routes to share three nominal shifts on aggregate time, even though no two of them could ever fit in a single \qty{8}{\hour} shift.

Constraint~\eqref{eq:sched-link} prevents a shift from being used unless its boat is also marked as used, keeping the boat count \(\sum_k u_k\) consistent with the shift assignment.

Constraints~\eqref{eq:sched-symmetry-boat} and~\eqref{eq:sched-symmetry-shift} break symmetry by forcing boats and the shifts within each boat to be used in increasing index order. Without them, the solver would explore equivalent permutations of the same assignment, which slows the search considerably.

The last three constraints,~\eqref{eq:sched-y-binary},~\eqref{eq:sched-u-binary} and~\eqref{eq:sched-z-binary}, keep the decision variables in their respective domains.

Once the model is solved, the optimised schedule can be rebuilt from the decision variables to form a route-to-boat-and-shift assignment, with a total working time and a shift count \(\sum_s z_{ks}\) for each boat \(k\). As with the \ac{psclp}, the model is implemented with Google OR-Tools \parencite{ortools} using its CP-SAT solver \parencite{cpsat}. Route durations \(\delta_r\) are converted from hours to integer seconds before being passed to the solver, since CP-SAT only accepts integer coefficients. A solver time limit of \qty{2}{\minute} per run is set, which is well above what is needed as the number of routes per day stays low and an optimal solution is returned in under a second.

\section{Results and Discussion}
\label{sec:analysis}

\subsection{Walking thresholds}
\label{subsec:walking_threshold}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=1\textwidth]{python/graphs/port_locations/psclp_utopia_choices.pdf}
  \caption{Summary of \ac{psclp} uptopia runs per walking theshold \(T_{\max}\), with the overall uptopia run.}
  \label{fig:utopia}
\end{figure}

As discussed in the Methodology, the \ac{psclp} was ran with multiple combinations of walking time threshold (\(T_{\max}\)) and minimum population covered by this walking time (\(\alpha)\). The results of the best runs by uptopia ranking by \(T_{\max}\) can be found in Figure~\ref{fig:utopia}, with also the best overall run. The two runs that were selected is (\(T_{\max}=15,\alpha=0.95\)) and (\(T_{\max}=28,\alpha=0.98\)) as the first is the best of its walking time group, and the second the best overall of all runs, again based on the utopia ranked described in Section~\ref{ssec:scenario_selection}.

The table also details who the relation between the ports needed to acheive a given walking time, as the smallest threshold forces the model to open a large amount of ports, but this amount reduces rapidly when reducing the threshold, going from \qtyrange{28}{17}{ports}.

It can also be observed that the total population covered by \(T_{\max}\) stays low for the marority of the threshold even if the model optimized based on it. These results signify that the population is still largy far away by walk from a canditate port, in all runs. At \(T_{\max}=15\), only \qty{46}{\percent} of the population is withing this threshold, the next \qty{5}{\min} adds \qty{10}{\percent}, reaching \qty{60}{\percent} only at \(T_{\max}=22\). Relaxing the threshold by \qty{7}{\min} has dropped the port count needed by \num{8}, or around \qty{29}{\percent}. This is why the utopia method was used to select a run, as their is too much dimension to easily chose a good scenario. Moreover, without any financial value for a openned port, or study on how long a citizen in Bergen would be willing to walk for its parcel, basing the choice on anything else than pure quantificantion of the run is impossible.

Furthermore, the average total population walking time to the nearest selected port can be seen in Table~\ref{anx:psclp_runs_details} in the \nameref{sec:appendix}. It stays within a small range between 19.68 and \qty{21.92}{\minute} across the selected runs, with a standard deviation below one minute, even though \(T_{\max}\) doubles from \qtyrange{15}{30}{\minute} and the number of ports drops from 28 to 17. Reducing the amount of ports by \num{10} therefore costs the average citizen around two minutes of additional walking. This suggests that the floor of the average walking time is set by Bergen's geography rather than by the strictness of the model. Further analysis could be done to determine if new ports location could be added or contruct to help reduce this metric.

For the rest of the thesis, the two runs will be refered as \texttt{t15} for the \ac{psclp} configuration (\(T_{\max}=15,\alpha=0.95\)) and \texttt{t28} for (\(T_{\max}=28,\alpha=0.98\)).

\subsection{Port Locations}
\label{subsec:port-location-results}

The layout of the chosen ports for both selected runs can be found in Figure~\ref{fig:run_port_maps}. It shows that the two runs have a lot of ports in common, and both shares the same map layout for the ports, with six clearly defined zones. This indicates that some candiate ports have good geographic position that can deserve a great portion of the population. The ports in \texttt{t28} are almost a subset of the ones in \texttt{t15}, with a few exceptions. This illustrate that a project like this could be implemented in different \(T_{\max}\), going from greater to smaller, by making a few changes to the model. Moreover, because we have clear geographic zones, some zones could be removed at the beginning and added later.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.48\textwidth]{maps/run_map_ports_15_a095.pdf}
  \hfill
  \includegraphics[width=0.48\textwidth]{maps/run_map_ports_28_a098.pdf}
  \caption{Selected ports and depot for \ac{psclp} runs \texttt{t15} and \texttt{t28}.}
  \label{fig:run_port_maps}
\end{figure}

The routes that were drawn to calculate the distances can also be seen in Figure~\ref{fig:run_port_maps} as the orange lines on the water. Again, these lines were only used to get relable distance between the ports, a real network would insure that they are actual optimal route between two ports, using advanced waterway knowledge.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{python/graphs/port_locations/coverage_thresholds.pdf}
  \caption{Total Population coverage for chosen runs by walking thresholds.}
  \label{fig:coverage_threshold}
\end{figure}

To better understand the effect of adding ports on the population coverage, the Figure~\ref{fig:coverage_threshold} can be observed. It cleary demonstrate that the addition of ports primarly increases the population coverage in the first \qtyrange{15}{20}{\min}, leaving the rest basibly unchanged. With \texttt{t28}, only \qty{36}{\percent} of the population was convered in \qty{15}{\min}, compared to only \qty{47}{\percent} with \texttt{t15}. However, overall, both runs have similay overall average population walking speed of \qty{19.68}{\min} for \texttt{t15} and \qty{20.73}{\min} for \texttt{t28}.

\subsection{Demand Scenarios}
\label{subsec:demand_scenarios}

As discussed, three demand scenarios were chosen to test the waterway network based on possible daily demands. The full year of daily demand and the three scenarios are shown in Figure~\ref{fig:yearly_demand}, where each day is coloured according to the scenario that could handle its demand.

Two patterns stand out. First, a within-week variation is visible, with the highest demand on Monday, a dip in the middle of the week, and a partial recovery by Friday. This pattern is not observed in the raw data but comes from the daily demand shares introduced in Section~\ref{subsec:weekly-to_daily-demand}, which spread the weekly total unevenly across the five weekdays. Second, a clear seasonality can be seen across the year. Demand rises sharply in November and December, most likely driven by the Christmas period in Norway, while noticeable drops appear in April and May, and again in late July. January stands out the most, with very low demand compared to the rest of the year. This could be the result of missing data in the PostNord dataset, although the weekdays of January are indeed present for the entire month. A more plausible explanation is the post-Christmas slowdown, when consumer and retail activity tend to drop at the start of the new year.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=1\textwidth]{python/graphs/demand_scenarios.pdf}
  \caption{Daily network parcel demand over the observed period, with weekdays only, classified according to the low, average, and peak demand scenarios.}
  \label{fig:yearly_demand}
\end{figure}

The low-demand scenario corresponds to a daily network demand of \qty{10732}{parcels}. The peak scenario is more than four times higher at \qty{44405}{parcels}, while the average scenario sits in between with \qty{21518}{parcels}. As shown in Figure~\ref{fig:yearly_demand}, very few days in the observed year fall within the low-demand range, with most of them concentrated at the very beginning of January. The low scenario therefore serves more as a reference for the quiet end of the demand range than as a typical operating day. By contrast, the average and peak scenarios bound the majority of the actual year, with most weekdays falling between the two thresholds.

\subsection{Port Demands}
\label{subsec:port-demand-results}

The full demand data for each scenarios of all the ports can be found in the \nameref{sec:appendix} in Table~\ref{anx:port_demand_scenarios_t15_a095} for \texttt{t15} and Table~\ref{anx:port_demand_scenarios_t28_a098} for \texttt{t28}. The distribution can also be visualize in Table~\ref{anx:demand_per_port}. As discussed earlier, the depot itself will also served as a demand port for household, letting them come and pick up their parcls, therefore its demand will not have to be transported by boat.

\subsection{Boat Routings}
\label{subsec:boat-routing-results}

The \ac{scvrp} model returns a lof ot results to discuss. These will be grouped by the \ac{psclp} selected solutions that they ran with (\texttt{t15} or \texttt{t28}), and a set of configurations (See Table~\ref{tab:cvrp_configs}). Each selected solutions having \num{27} results. All configurations were ran for \qty{1}{hour} to make sure that the solver has enought time to find a good solution. Each runs resulted in a succesful solution, altought none were proven optimal by the solver. Most of them found their best solution in a matter of seconds and the rest of the run duration accomplish nothing, aldought others, like in \texttt{t15} (\(Q=2500, e=4)\), (\(Q=1500, e=3)\) succesfully found betters at \qty{50}{\minute}. An interesting observation is that the configurations that were able to take advantage of the \qty{1}{\hour} run time are not the same in both \texttt{t15} and \texttt{t28}. Indeed, in \texttt{t28}, the configurations with \(Q=500\) were the last one to found their best solutions.

\subsubsection{Total Number of Routes}
\label{ssec:total-number-routes}

In Figure~\ref{fig:total_routes_and_median_capacity}, the number of routes needed to handle the configurations can be seen, combined with the median capacity utilization as a percentage, for all the routes.

\begin{figure}[htbp]
  \centering
  % First image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t15_a095_routing_capacity_utilization.pdf}
  \hfill
  % Second image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t28_a098_routing_capacity_utilization.pdf}

  % The single shared caption
  \caption{Number of routes required under the low, average, and peak demand scenarios, and corresponding average capacity utilization, for each configuration and demand scenario.}
  \label{fig:total_routes_and_median_capacity}
\end{figure}

The first observation that can be made is obvious, the number of routes is directly inversly correlated with the boat parcel capacity. As expected, the less a boat can carry parcel for a given route, the more it will have to make route to carry the demand.

The second observation is that the number of routes are not really affected by the number of employees (\(e\)), in both \texttt{t15} and \texttt{t28}, only with \(Q=2500\) made a difference, both resulting in \(e = {3,4}\) having \num{4} less routes than with \(e=2\). At first, this could look like an error, as more employees means faster parcel handling time, wich should result in shorter routes, therefore potential an different best solutions. However, the objective of the solver is not to minimise the amount of routes, but to minimize the total travel time for a given day. As a result, the parcel handling time are not taken into considuration by the objective function, wich in returns explain the number of routes that are mostly not affecred in these results. Only with the largest boat capacity of \(Q=2500\) that the reduction in handling time made such a difference that the solver found a more optimal routing network.

The third observation is that the median capacity utilization in the routes varies a lot depdending on the boat capacity (\(Q\)). Looking at \(Q=2500\) for both \texttt{t15} and \texttt{t28}, a lower median utilization percentage can be seen compared to other capacities. Looking at the lowest median, \(e=2\), suggest that with only two employees, the demand as to be splitted in a bigger number of routes as the handling time is too large, the route parcel capacity is therefore limited not by the boat capacity, but by the speed of the parcel handling. Adding one more employee (\(e=3\)), greatly helps the median, as it shore to above \qty{90}{\percent} fore all demand scenario. However, the improvement stops at \(e=3\), as it can be seen that with \(e=4\), the median is the same per demand scenario as with the previous \(e\) for \texttt{t28} and stays around \qty{95}{\percent} for \texttt{t15}. This can be an indication that the \(Q=2500\) is too large of a capacity for this context, as adding more employees does not impact very much the median capacity utilization. The exact opposite can be seen with \(Q=500\), as the median capacity utilization is very sits around \qtyrange{95}{100}{\percent} in all configurations and scenarios, seignaling that the capacity is a clear constraints because the route are often quite full.

In general, it could be argued that because \(Q=2500\) has the worst capacity utilization, its capacity is too large and the routes are mostly limited by the route total duration constrains of \qty{8}{\hour} and not the boat's capacity. The same logic can be applied to \(Q=500\), as its median capacity coverage utilization is always very hight, pared with the fact that the number of routed needed is very large compared to the other capacites, this could mean that this capacity is too small for the demand that is needed. However, because of its few number of routes, the medians for \(Q=2500\) needs to be interpreted with caution, \(Q=500\) having a lot more routes, is a lot more reliable.

\subsubsection{Total Route Duration}
\label{ssec:total-route-duraton}

The next metric that can be discuess is the total travel time of the routes. In Figure~\ref{fig:total_and_median_routing_time}, these duration are shown by runs and configurations, with a visual split for the total navigation time and parcel handling time. Lines representing the median route time in hours are also present for each demand scenarios.

\begin{figure}[htbp]
  \centering
  % First image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t15_a095_routing_travel_time.pdf}
  \hfill
  % Second image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t28_a098_routing_travel_time.pdf}

  % The single shared caption
  \caption{Total route time by configuration and demand scenario, separated into navigation time and parcel handling time.}
  \label{fig:total_and_median_routing_time}
\end{figure}

The first observation that can be made is clear, as for the total routes needed, the total route time doubles across all confugrations as the demand scnerios increase. This observation, pared with the scenonality seen in Figure~\ref{fig:yearly_demand}, signify how much the system will have to expand to be able to handle the peek demands. However, in these periods, the number of employees per boat could be increased to help reduce the overall total route time.

The second observation is that compared with the results from Table~\ref{fig:total_routes_and_median_capacity}, where the total number of routes did not really change when \(e\) changed, the real affect can now be seen. For exemple, in \texttt{t15}, all employees count had the same amount of routes per demand scenario. However, it can be seen here that even thought the amount of routes are equal, the total route duration are smaller.

A third observation is that the reduction in total route time is not directly proportional to the number of employees added to the crew. For example, in \texttt{t28}, going from \(e=2\) to \(e=3\) reduced the average total route time by \qty{26}{\percent}, while going from \(e=3\) to \(e=4\) only achieved a reduction of \qty{17}{\percent}. This diminishing return follows directly from the model, since only the parcel handling portion of the total time scales with \(e\). The handling time at a port is \(d_i \tau / e\), while the navigation and manoeuvring times do not depend on \(e\) at all. The same pattern also explains why the reduction is stronger at higher boat capacities, where each route carries more parcels and the handling share of the total time is larger, leaving more work for the additional crew member to absorb.

A fourth observation is that the reduction in total route time from increasing the boat capacity \(Q\) is also not linear. Going from \(Q=500\) to \(Q=1500\) reduced the average total route time by \qty{29}{\percent}, while going from \(Q=1500\) to \(Q=2500\) only achieved a reduction of \qty{7}{\percent}. This is consistent with the capacity utilisation results in Figure~\ref{fig:total_routes_and_median_capacity}, which already showed that the largest boats were not filled to their full capacity. The extra space offered by \(Q=2500\) over \(Q=1500\) is therefore mostly unused and brings little additional benefit on the total route time.

A final obervation can be made about the median route duration. Looking at \(Q=500\) reveils that the routes are very short, from \qtyrange{2}{3}{\hour} only, while the maximum contrains is set at \qty{8}{\hour}. This shows that with this capacity, the route are limited not by the travel time but the capacity of the boat, wich further supports the capacity utilization of Figure~\ref{fig:total_routes_and_median_capacity}. The exact opposite can be observed.

To summarise, the total route time follows a clear pattern of diminishing returns for both the crew size \(e\) and the boat capacity \(Q\). The parcel handling dominates the total time and is the only component that responds to a larger crew, while the navigation time stays fixed regardless of the configuration. Each new employee, or each step up in capacity, therefore contributes less than the previous one, and the largest boats remain partly empty. Combined with the strong seasonal variation of the demand, this suggests that the most efficient strategy is a balance rather than a maximum, with the crew size potentially adjusted between low and peak periods instead of permanently oversizing the fleet.

\subsubsection{Total Route Distances}
\label{ssec:total-route-distances}

The next metric that can be discuss is the total route distances in nautical mile. This is important because in a real scenario, boats do have a maximum distance range and needs to be refuelled or recharged.  However, as previously stated, the model did not have any contraints about a maximum route distance, only about its duration and its total capacity. The value for this metric can be seen in Figure~\ref{fig:total_and_median_distances}.

\begin{figure}[htbp]
  \centering
  % First image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t15_a095_total_distance_per_run.pdf}
  \hfill
  % Second image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t28_a098_total_distance_per_run.pdf}

  % The single shared caption
  \caption{Total sailing distance and average distance per route for each configuration and demand scenario.}
  \label{fig:total_and_median_distances}
\end{figure}

The first observation that can be made is that total distances between \(Q=500\) and \(Q=1500\) drops drasticly. This is explained as the total routes also decreases, as seen in Table~\ref{fig:total_routes_and_median_capacity}. More routes for the same total demand mean more navigation made to and from the depot, wich served no demand and only increase rapidly the distance. Having more capacity therefore reduces the number of routes, wich tends to reduce the total travelled distances. In contrast, as with the others metric already talked about previsouly, the changes from \(e=2\) to \(e=3\) are much larger than the one from \(e=3\) to \(e=4\). This reenforce the assomption that \(Q=500\) may be too small of a capacity, and \(Q=2500\) too large.

The second observation is that the median distance per route behaves differently across scenarios. For average and peak demand, it stays close to \qty{22}{nmi} up to \(Q=1500\) in both runs. At \(Q=2500\), the average climbs fast while the peak keeps a slow and steady rise. The low demand scenario is less predictable. At \(Q=1500\) its median stays flat across all values of \(e\), around \qty{26}{nmi} in \texttt{t15} and \qty{24}{nmi} in \texttt{t28}. This suggests the same routes are kept regardless of \(e\). At \(Q=2500\), \texttt{t15} and \texttt{t28} diverge. For low demand, \texttt{t15} drops to about \qty{23}{nmi}, while \texttt{t28} climbs to a peak of \qty{28}{nmi} at \(e=2\) before falling back to around \qty{25}{nmi}. The average scenario also splits. In \texttt{t15} it peaks at \qty{25.7}{nmi} at \(e=3\) and drops slightly at \(e=4\), while in \texttt{t28} it keeps climbing steadily. Most of this variation comes from the small number of routes at \(Q=2500\). With only around \qty{5}{routes}, removing a single one of \qty{27}{nmi} between \(e=2\) and \(e=3\)  is enough to shift the median by several nautical miles.

To summarise, capacity is the main driver of total distance. The biggest drop happens between \(Q=500\) and \(Q=1500\), while pushing further to \(Q=2500\) brings smaller gains. The number of employees \(e\) barely changes anything at \(Q=500\) and \(Q=1500\) since the model minimizes navigation time and not distance, and only starts to matter at \(Q=2500\), where the small number of routes also makes the median per route sensitive to small changes.

\subsection{Boat Scheduling}
\label{subsec:boat-scheduling}

The next step of the methodology was to optimize the routes in boats and shifts. As this modeling does not require any manual added information, like for the port distances after the \ac{psclp}, the model was run on all routes solution of the model. Two key metrics will be shown and discussed, the number of boats and the number of employees per day needed to served the routes for each configurations for both \texttt{t15} and \texttt{t28}. Both metrics can be seen in Figure~\ref{fig:boats_and_employees}.

\begin{figure}[htbp]
  \centering
  % First image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t15_a095_routing_routes_per_config.pdf}
  \hfill
  % Second image
  \includegraphics[width=0.49\textwidth]{python/graphs/routing/t28_a098_routing_routes_per_config.pdf}

  % The single shared caption
  \caption{Number of boats and employees needed for all configurations and demand scenarios.}
  \label{fig:boats_and_employees}
\end{figure}


\subsubsection{Boats Needed}
\label{ssec:boats-needed}

The first observation concerns the low number of boats needed compared to the number of routes from Figure~\ref{fig:total_routes_and_median_capacity}. For example, with \(Q=500\) at peak demand, all \num{76} routes are covered by only \num{10} boats. This works because the routes are very short, so many of them can be chained on the same boat within the \qty{24}{\hour} daily window. As routes get longer, fewer of them fit on a single boat. The variation in boat count between demand scenarios stays high, although it shrinks when more employees are assigned per boat, since a larger crew shortens each route.

The second observation is that the number of boats needed across \(Q\) values within the same demand scenario is roughly constant. For both low and average demand, the standard deviation stays below \num{1}, with values between \numrange{1}{3} boats for low demand and \numrange{2}{5} boats for average demand. The peak scenario shows more variation, with the boat count ranging from \num{5} to \num{10} and a standard deviation of about \num{1.8}. On top of that, both \texttt{t15} and \texttt{t28} require a very similar number of boats, which means the capital cost of the fleet cannot really be used to decide between the two routing scenarios. Moving from average to peak demand, however, increases the boats needed by \qty{95}{\percent} on average, which confirms that demand level is by far the strongest driver of fleet size.

The final observation for the boats needed is that the number of employees per boat still play a role in reducing it on higher demand scenarios, while only being able to reduce it by \num{1} on the lower demand scenario wich is still around around \qtyrange{33}{50}{\percent} depending on the boat capacity. Nevertheless, for all scenarios, even if the smallest amount of employees is used for the low demand scenario, and the hiest for the peak, it would still require the amount if boats to be at least doubled to serve all demand scenarios for a given \(Q\).

\subsubsection{Employees Needed}
\label{ssec:employees-needed}

This metric helps identify the impact that different values of \(e\) have on the total employee count for the day. The first observation is that increasing \(e\) almost always increases the total number of employees needed, with one exception at \(Q=1500\). For this boat capacity, in every demand scenario, going from \(e=2\) to \(e=3\) raises the employee count, but going from \(e=3\) to \(e=4\) lowers it by an average of \qty{22}{\percent}, while also reducing the number of boats needed. As shown in Figure~\ref{fig:total_routes_and_median_capacity}, the total number of routes did not change with different values of \(e\) for this \(Q\). However, Figure~\ref{fig:total_and_median_routing_time} showed that both the total and the median routing time decreased. This means that the extra employees made the routes shorter, which allowed the same crew to cover more routes during the day, and therefore reduced the total employee count.

For the other values of \(Q\), the behaviour is more straightforward. Going from \(e=2\) to \(e=3\) increases the total employees needed by an average of \qty{20}{\percent}, while reducing the number of boats by \qty{18}{\percent} on average for \(Q=500\) and \qty{7}{\percent} for \(Q=2500\). For \(Q=2500\), the boat reduction only happens between \(e=2\) and \(e=3\) and disappears between \(e=3\) and \(e=4\). This shows that even when adding more employees makes the routes shorter, the gain is not always large enough to free up an entire boat.

In a real-world setting, this tradeoff between fleet size and crew size could be settled on a cost basis, as long as the daily operating cost of a boat and the daily cost of an employee are both known. Without those figures, only assumptions can be made here, and the best configuration stays dependent on the cost structure a future operator works with.


\subsection{Summary of Results}
\label{subsec:results-summary}

In summary, as both \texttt{t15} and \texttt{t28} serves the same total amount of demand, the logistics between the two scenarios do not change very much. It would require similar amount of boats and employees, who would travel raoughly the same distances, for the same amount of time in the same amount if routes. These results can be seen in Figure~\ref{tab:results-sum} Nevertheless, the limitation of budget for the amount of boats and employees would probobly make the desicion.

\begin{table}[htbp]
  \centering
  \caption{Average percentage change in routing metrics when comparing \texttt{t28} to \texttt{t15}, by demand scenario and overall. Negative values indicate lower values for the 28-minute solution.}
  \label{tab:results-sum}

  \begin{tabular}{l|rrrr}
    \toprule
    \textbf{Variable}           & \textbf{Low} & \textbf{Average} & \textbf{Peak} & \textbf{Overall} \\
    \midrule
    Routes                      & --           & -3\%             & -4\%          & -3\%             \\
    Route duration              & -4\%         & -3\%             & -3\%          & -4\%             \\
    Route distance              & -2\%         & 0\%              & -2\%          & -1\%             \\
    Boats                       & -6\%         & -6\%             & -7\%          & -6\%             \\
    Employees                   & -6\%         & --               & -4\%          & -4\%             \\
    Median capacity utilization & -2\%         & 1\%              & 1\%           & --               \\
    Median route time           & -4\%         & -1\%             & 2\%           & -1\%             \\
    Median distance per route   & -2\%         & -2\%             & -1\%          & -2\%             \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{Average routing performance metrics for the \texttt{t15} and \texttt{t28} port-location solutions, reported by demand scenario.}
  \label{tab:detailed_kpis}

  \begin{tabular}{l|rrrrrr}
    \toprule
                                    & \multicolumn{2}{c}{\textbf{Low}} & \multicolumn{2}{c}{\textbf{Average}} & \multicolumn{2}{c}{\textbf{Peak}}                                                                \\
    \cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
    \textbf{Variable}               & \textbf{t15}                     & \textbf{t28}                         & \textbf{t15}                      & \textbf{t28}       & \textbf{t15}       & \textbf{t28}       \\
    \midrule
    Routes                          & 9.44                             & 9.44                                 & 19.56                             & 18.89              & 39.78              & 38.44              \\
    Route duration                  & 34.00                            & 32.57                                & 67.74                             & 65.76              & 137.96             & 133.39             \\
    Route distance (nmi)            & 185.86                           & 180.90                               & 335.51                            & 336.97             & 662.33             & 649.13             \\
    Boats                           & 2.00                             & 1.89                                 & 3.78                              & 3.56               & 7.33               & 6.89               \\
    Employees                       & 14.67                            & 13.56                                & 28.56                             & 28.56              & 58.33              & 55.78              \\
    Median capacity utilization     & \qty{95}{\percent}               & \qty{93}{\percent}                   & \qty{94}{\percent}                & \qty{95}{\percent} & \qty{96}{\percent} & \qty{97}{\percent} \\
    Median route time (hours)       & 4.88                             & 4.71                                 & 4.60                              & 4.56               & 4.63               & 4.74               \\
    Median distance per route (nmi) & 24.23                            & 23.62                                & 22.89                             & 22.52              & 21.64              & 21.35              \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Conclusion}
\label{sec:conclusion}

\subsection{Summary of Findings}
\label{subsec:summary-of-findings}

\subsection{Answer to the Research Question}
\label{subsec:answer-to-the-research-question}

\subsection{Further Research}
\label{subsec:further-research}

\clearpage
\printbibliography
\appendix

\section{Appendix}
\label{sec:appendix}

\counterwithin{figure}{section}
\counterwithin{table}{section}
\counterwithin{equation}{section}

\subsection{Population}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{python/graphs/population_by_district.pdf}
  \caption{City of Bergen population distribution by district in 2020.}
  \label{anx:population-distribution}
\end{figure}

\subsection{Ports}

\begin{table}[htbp]
  \centering
  \caption{Total \ac{osm} candidate ports nodes aggregated by tag.}
  \label{anx:port_nodes_by_tag}

  \begin{tabular}{l r}
    \toprule
    \textbf{Infrastructure} & \textbf{Node Count} \\
    \midrule
    pier                    & 286                 \\
    ferry\_terminal         & 10                  \\
    quay                    & 10                  \\
    mixed*                  & 4                   \\
    \bottomrule
  \end{tabular}

  \vspace{1ex}
  {\raggedright \footnotesize * A node is classified as mixed when port locations of different tags were snaped to the same \ac{osm} node.\par}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{graphs/walking_time_coverage.pdf}
  \caption{City of Bergen population density in 2020.}
  \label{anx:map_density}
\end{figure}

\subsection{Demand}

\begin{table}[htbp]
  \centering
  \caption{Private market consumer parcel courier usage \parencite{nkom_post_2025}}
  \label{anx:private_parcel_usage}

  \begin{tabular}{l r r}
    \toprule
    \textbf{Company}         & \textbf{Survey Value (\%)} & \textbf{Normalized Value (\%)} \\
    \midrule
    Posten Norge AS/Bring AS & 85                         & 55.56                          \\
    PostNord AS              & 30                         & 19.61                          \\
    Helthjem AS              & 21                         & 13.73                          \\
    Vet ikke                 & 8                          & 5.23                           \\
    Instabox Norway          & 4                          & 2.61                           \\
    Andre                    & 3                          & 1.96                           \\
    Porterbuddy              & 2                          & 1.31                           \\

    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{PostNord annual parcel demand \parencite{pn_a_2020,pn_a_2021,pn_a_2022,pn_a_2023,pn_a_2024,pn_a_2025}}
  \label{anx:yearly_pn_demand}

  \begin{tabular}{l r}
    \toprule
    \textbf{Year} & \textbf{Parcel Demand (million)} \\
    \midrule
    2020          & 198                              \\
    2021          & 226                              \\
    2022          & 245                              \\
    2023          & 243                              \\
    2024          & 245                              \\
    2025          & 281                              \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{equation}\label{eq:postnord_cagr}
  \text{CAGR} = \left( \frac{D_{2025}}{D_{2022}} \right)^{\frac{1}{3}} - 1 \approx 4.68\%
\end{equation}
Equation~\ref{eq:postnord_cagr} outlines the \ac{cagr} formula for PostNord parcel volume, where \(D_{2025}\) and \(D_{2022}\) are the company yearly demand for those years. See Table~\ref{anx:yearly_pn_demand} for all yearly demand.

\begin{table}[htbp]
  \centering
  \caption{Sequential adjustments and dataset filtration of parcel demand volumes.}
  \label{anx:parcel_volumes}
  \begin{tabular}{lrr}
    \toprule
    \textbf{Data Cleaning / Scaling Step}    & \textbf{Parcel Volume} & \textbf{Delta Variance} \\
    \midrule
    PostNord 2020 Baseline                   & 1,628,441              & --                      \\
    PostNord 2026 Projection                 & 2,419,134              & \(+790,693\)            \\
    With Total Market Share (20\%)           & 12,336,226             & \(+9,917,092\)          \\
    Only Households                          & 8,125,871              & \(-4,210,355\)          \\
    Operational Dataset (Filtered Districts) & 5,977,449              & \(-2,148,422\)          \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{7-Day Split Weekly demand \parencite{dailydemand} and new 5-Day Split.}
  \label{anx:day_shares}

  \begin{tabular}{lrr}
    \toprule
    \textbf{Day of Week} & \textbf{7-Day Split} & \textbf{5-Day Split*} \\
    \midrule
    Monday               & 0.1965               & 0.1979                \\
    Tuesday              & 0.2370               & 0.2370                \\
    Wednesday            & 0.2047               & 0.2047                \\
    Thursday             & 0.1901               & 0.1901                \\
    Friday               & 0.1703               & 0.1703                \\
    Saturday             & 0.0014               & --                    \\
    Sunday               & 0.0000               & --                    \\
    \bottomrule
  \end{tabular}

  \vspace{1ex}
  {\raggedright \footnotesize * The 7-Day Split' Saturday volume is given to Monday's allocation for the 5-day weekday operations.\par}
\end{table}

\subsection{Results}

\subsubsection{Port Demands}

\begin{table}[htbp]
  \centering
  \caption{Port demand scenario data for run \(T_{max}=28\) and \(\alpha=0.98\) (in parcels).}
  \label{anx:port_demand_scenarios_t28_a098}
  \begin{tabular}{lrrr}
    \toprule
    \textbf{OSM Node} & \textbf{Low Scenario} & \textbf{Avg Scenario} & \textbf{Peak Scenario} \\
    \midrule
    17952828          & 104                   & 187                   & 400                    \\
    113834140         & 41                    & 134                   & 285                    \\
    863391850         & 705                   & 1,454                 & 3,279                  \\
    3685706940        & 927                   & 1,539                 & 3,135                  \\
    3689983240        & 40                    & 64                    & 189                    \\
    3695357841        & 125                   & 194                   & 445                    \\
    3977652985        & 2,356                 & 3,785                 & 7,627                  \\
    4105134356        & 834                   & 1,837                 & 3,563                  \\
    4118639614        & 961                   & 2,438                 & 5,180                  \\
    4234389320        & 760                   & 1,454                 & 2,661                  \\
    4872700891        & 600                   & 1,429                 & 2,739                  \\
    4912219155        & 240                   & 424                   & 951                    \\
    4933230383        & 607                   & 1,351                 & 3,410                  \\
    4945276305        & 1,063                 & 2,291                 & 4,728                  \\
    4973225641        & 124                   & 219                   & 523                    \\
    6131737864        & 506                   & 1,054                 & 1,918                  \\
    6508381749        & 103                   & 228                   & 540                    \\
    9351325110        & 199                   & 507                   & 871                    \\
    9763645545        & 118                   & 255                   & 578                    \\
    11083764916       & 319                   & 674                   & 1,383                  \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{Port demand scenario data for run \(T_{max}=15\) and \(\alpha=0.95\) (in parcels).}
  \label{anx:port_demand_scenarios_t15_a095}
  \begin{tabular}{lrrr}
    \toprule
    \textbf{OSM Node} & \textbf{Low Scenario} & \textbf{Avg Scenario} & \textbf{Peak Scenario} \\
    \midrule
    17952828          & 96                    & 167                   & 349                    \\
    29600478          & 232                   & 366                   & 720                    \\
    29926943          & 50                    & 95                    & 248                    \\
    113834140         & 18                    & 59                    & 131                    \\
    3685706940        & 696                   & 1,168                 & 2,417                  \\
    3689983240        & 40                    & 64                    & 189                    \\
    3902076021        & 16                    & 39                    & 117                    \\
    3977652985        & 2,068                 & 3,259                 & 6,486                  \\
    4086516345        & 421                   & 768                   & 1,598                  \\
    4105134356        & 763                   & 1,687                 & 3,256                  \\
    4118639614        & 1,076                 & 2,616                 & 5,571                  \\
    4234389320        & 754                   & 1,439                 & 2,622                  \\
    4268515208        & 101                   & 170                   & 409                    \\
    4333918395        & 115                   & 248                   & 565                    \\
    4743388682        & 688                   & 1,420                 & 3,214                  \\
    4872700891        & 499                   & 1,191                 & 2,271                  \\
    4933230383        & 634                   & 1,409                 & 3,519                  \\
    4945276305        & 803                   & 1,813                 & 3,737                  \\
    6015545311        & 274                   & 622                   & 1,267                  \\
    6131737864        & 392                   & 725                   & 1,356                  \\
    6508381749        & 94                    & 203                   & 480                    \\
    10124360518       & 11                    & 23                    & 60                     \\
    10696136475       & 75                    & 191                   & 360                    \\
    10700534562       & 128                   & 223                   & 522                    \\
    10876181717       & 296                   & 710                   & 1,253                  \\
    11083764916       & 322                   & 681                   & 1,397                  \\
    12273007575       & 21                    & 40                    & 106                    \\
    12941923336       & 51                    & 111                   & 233                    \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering

  \includegraphics[width=0.95\textwidth]{python/graphs/port_demands/t15_a095_demand_scenarios_per_port.pdf}

  \vspace{0.6cm}

  \includegraphics[width=0.95\textwidth]{python/graphs/port_demands/t28_a098_demand_scenarios_per_port.pdf}

  \caption{Distribution of daily parcel demand across selected port and depot under the low, average, and peak demand scenarios. The upper graph corresponds to \(T_{\max}=15\) min and \(\alpha=0.95\), while the lower graph corresponds to \(T_{\max}=28\) min and \(\alpha=0.98\).}
  \label{anx:demand_per_port}
\end{figure}

\begin{figure}[htbp]
  \centering
  % First image
  \includegraphics[width=0.48\textwidth]{python/graphs/port_demands/t15_a095_demand_per_zone.pdf}
  \hfill
  % Second image
  \includegraphics[width=0.48\textwidth]{python/graphs/port_demands/t28_a098_demand_per_zone.pdf}

  % The single shared caption
  \caption{Yearly demand by port zones.}
  \label{anx:demand_zones}
\end{figure}

\subsubsection{\ac{psclp} Runs}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.48\textwidth]{python/graphs/port_locations/t15_a095_coverage_cumulative.pdf}
  \hfill
  \includegraphics[width=0.48\textwidth]{python/graphs/port_locations/t28_a098_coverage_cumulative.pdf}
  \caption{Cummulative total population coverage, grouped by districts and all, by walking time.}
  \label{anx:coverage_cumulative}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{PSCLP Utopia Choices by Walking-Time Threshold.}
  \label{anx:psclp_runs_details}

  \begin{tabular}{lrrrr}
    \toprule
    \textbf{\(T_{\max}\)} & \textbf{\(\alpha\)} & \textbf{Ports Needed} & \textbf{Population Covered} & \textbf{Avg Walking Time (min)} \\
    \midrule
    15                    & 0.95                & 28                    & 46.45\%                     & 19.68                           \\
    16                    & 0.94                & 25                    & 48.43\%                     & 19.97                           \\
    17                    & 0.95                & 25                    & 51.53\%                     & 19.92                           \\
    18                    & 0.95                & 24                    & 53.73\%                     & 20.09                           \\
    19                    & 0.96                & 25                    & 56.08\%                     & 19.91                           \\
    20                    & 0.94                & 21                    & 56.80\%                     & 20.29                           \\
    21                    & 0.95                & 21                    & 59.46\%                     & 20.34                           \\
    22                    & 0.96                & 21                    & 62.44\%                     & 20.51                           \\
    23                    & 0.95                & 19                    & 63.67\%                     & 20.88                           \\
    24                    & 0.96                & 20                    & 66.70\%                     & 20.71                           \\
    25                    & 0.96                & 19                    & 68.68\%                     & 20.83                           \\
    26                    & 0.96                & 18                    & 70.52\%                     & 21.09                           \\
    27                    & 0.96                & 18                    & 72.65\%                     & 21.09                           \\
    28                    & 0.96                & 18                    & 74.13\%                     & 21.19                           \\
    28                    & 0.98                & 20                    & 75.78\%                     & 20.73                           \\
    29                    & 0.97                & 18                    & 76.66\%                     & 21.44                           \\
    30                    & 0.97                & 17                    & 78.42\%                     & 21.92                           \\
    \bottomrule
  \end{tabular}
\end{table}

\end{document}

